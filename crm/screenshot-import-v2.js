(() => {
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const modal=$('#customerModal'),form=$('#customerForm');if(!modal||!form)return;
  let currentFile=null,objectUrl=null,nativeBusy=false;
  const fields=name=>form.elements.namedItem(name);
  const productNames=['Tirzepatide','Retatrutide','GHK-Cu','GHK CU','BPC-157','BPC157','5-Amino-1MQ','5 Amino 1MQ','Ipamorelin','Tesamorelin','AOD9604','AOD-9604','CJC-1295','CJC1295','Semaglutide','TB-500','TB500','MOTS-C','MOTSC','Epithalon','KPV','PT-141','Melanotan','NAD+','SS-31','ARA-290','AICAR'];
  const uiNoise=/^(whatsapp|online|typing|today|yesterday|video call|voice call|search|message|type a message|business account|last seen|forwarded|read more|mute|block|report)$/i;
  const uniq=a=>[...new Set(a.filter(Boolean))],clean=v=>String(v||'').replace(/[|<>©®]/g,' ').replace(/\s+/g,' ').trim();
  function setField(name,value){const el=fields(name);if(!el||value===undefined||value===null||value==='')return;if(el.tagName==='SELECT'){const option=[...el.options].find(o=>o.value.toLowerCase()===String(value).toLowerCase()||o.text.toLowerCase()===String(value).toLowerCase());if(!option)return;el.value=option.value}else el.value=value;el.classList.add('field-autofilled');setTimeout(()=>el.classList.remove('field-autofilled'),3200)}
  function setStatus(title,hint='',progress=null){$('#ocrStatus').textContent=title;if(hint)$('#ocrHint').textContent=hint;if(progress!==null){const p=Math.max(0,Math.min(100,Math.round(progress)));$('#ocrProgressBar').style.width=p+'%';$('#ocrProgressText').textContent=p?`${p}%`:''}}
  function detectPhone(text){const m=text.match(/(?:\+\s?\d{1,3}[\s().-]*)?(?:\d[\s().-]*){7,14}/g)||[],a=m.map(x=>x.trim()).filter(x=>x.replace(/\D/g,'').length>=8);return a.find(x=>x.includes('+'))||a[0]||''}
  function countryFrom(text,phone){const t=' '+text.toLowerCase()+' ',named=[[' canada ','Canada'],[' mexico ','Mexico'],[' australia ','Australia'],[' united kingdom ','United Kingdom'],[' uk ','United Kingdom'],[' france ','France'],[' germany ','Germany'],[' spain ','Spain'],[' italy ','Italy'],[' united states ','United States'],[' usa ','United States']];for(const [k,v] of named)if(t.includes(k))return v;const p=phone.replace(/[\s().-]/g,'');if(p.startsWith('+52'))return'Mexico';if(p.startsWith('+61'))return'Australia';if(p.startsWith('+44'))return'United Kingdom';if(p.startsWith('+33'))return'France';if(p.startsWith('+49'))return'Germany';if(p.startsWith('+34'))return'Spain';if(p.startsWith('+39'))return'Italy';if(p.startsWith('+1'))return'United States';return''}
  function detectProduct(text){const low=text.toLowerCase(),found=productNames.find(p=>low.includes(p.toLowerCase()));if(!found)return'';const map={'GHK CU':'GHK-Cu','BPC157':'BPC-157','5 Amino 1MQ':'5-Amino-1MQ','AOD-9604':'AOD9604','CJC1295':'CJC-1295','TB500':'TB-500','MOTSC':'MOTS-C'},name=map[found]||found,idx=low.indexOf(found.toLowerCase()),around=text.slice(Math.max(0,idx-25),Math.min(text.length,idx+found.length+40)),mg=around.match(/\b\d+(?:\.\d+)?\s*mg\b/i);return mg?`${name} ${mg[0].replace(/\s+/g,'')}`:name}
  function detectType(text){const t=text.toLowerCase();if(/oem|private\s*label|custom\s*(label|packaging|brand)/.test(t))return'OEM/定制';if(/distributor|reseller|wholesale|wholesaler|bulk\s*(order|buy|purchase)/.test(t))return'经销商';if(/retailer|retail store|shop owner|online store/.test(t))return'零售商';if(/research institute|laboratory|lab buyer|procurement|purchasing department|company buyer/.test(t))return'机构采购';if(/research use|research customer|researcher/.test(t))return'科研客户';if(/brand owner|our brand|my brand/.test(t))return'品牌方';return'个人客户'}
  function detectTags(text){const t=text.toLowerCase(),tags=[];if(/price|cost|how much|quotation|quote|discount/.test(t))tags.push('价格咨询');if(/moq|minimum order|min\. order/.test(t))tags.push('MOQ关注');if(/coa|hplc|\bms\b|certificate|purity|test report/.test(t))tags.push('文件关注');if(/bulk|wholesale|distributor|reseller|large order|monthly order/.test(t))tags.push('批量采购');if(/shipping|delivery|fedex|tracking|arrive|transit/.test(t))tags.push('物流关注');if(/sample|testing sample|trial order/.test(t))tags.push('样品咨询');if(/payment|paypal|bank transfer|wire transfer|pay by/.test(t))tags.push('付款关注');if(/private label|oem|custom label|custom packaging/.test(t))tags.push('定制需求');return uniq(tags)}
  function detectName(lines,phone){const blacklist=/\b(price|product|shipping|payment|tirzepatide|retatrutide|ghk|bpc|mg|hello|hi|thanks|order|bulk|wholesale)\b/i;for(const raw of lines.slice(0,22)){const line=clean(raw);if(!line||line.length<2||line.length>42||uiNoise.test(line)||blacklist.test(line))continue;if(phone&&line.includes(phone))continue;if(/\d{1,2}:\d{2}/.test(line)||(line.match(/\d/g)||[]).length>3)continue;if(/^[A-Za-zÀ-ÿ][A-Za-zÀ-ÿ .'-]{1,38}$/.test(line))return line}return''}
  function detectMessage(lines,name){const a=lines.map(clean).filter(x=>x.length>=7&&x.length<=280&&!uiNoise.test(x)&&x!==name&&!/^\+?[\d\s().-]{8,}$/.test(x)&&!/^\d{1,2}:\d{2}/.test(x));return a.find(x=>/price|cost|how much|tirzepatide|retatrutide|ghk|bpc|bulk|wholesale|coa|shipping|sample|order|mg/i.test(x))||a.slice(-1)[0]||''}
  function parse(text){const lines=text.split(/\r?\n/).map(clean).filter(Boolean),phone=detectPhone(text),country=countryFrom(text,phone),product=detectProduct(text),type=detectType(text),tags=detectTags(text),name=detectName(lines,phone),message=detectMessage(lines,name),grade=type==='机构采购'&&/monthly|long[- ]?term|supplier|procurement|bulk/i.test(text)?'S':tags.includes('批量采购')||/ready to order|place an order|payment/i.test(text)?'A':'B',notes=['截图/文字辅助建档'];if(message)notes.push(`客户消息：${message}`);if(tags.length)notes.push(`关注点：${tags.join('、')}`);if(phone.startsWith('+1')&&!/canada|united states|usa/i.test(text))notes.push('号码为 +1，请人工确认美国/加拿大');return{name,phone,country,product,type,tags,grade,message,note:notes.join('；')+'。'}}
  function apply(r){setField('name',r.name);setField('contact',r.phone);setField('country',r.country);setField('product',r.product);setField('customerType',r.type);setField('grade',r.grade);setField('source','Facebook Ads → WhatsApp');setField('traits',r.tags.join(', '));setField('firstMessage',r.message);setField('note',r.note);const n=[r.name,r.phone,r.country,r.product,r.message].filter(Boolean).length;setStatus('已解析，请人工确认',`已提取 ${n} 项关键信息。系统不会自动保存，确认无误后再创建客户。`,100)}
  function ensureTextAssist(){const panel=$('.ocr-panel');if(!panel||$('#assistTextInput'))return;const box=document.createElement('div');box.style.cssText='margin-top:12px;padding-top:12px;border-top:1px solid #e4e7ec';box.innerHTML='<label style="display:block;font-size:12px;font-weight:600;margin-bottom:6px">聊天文字辅助解析</label><textarea id="assistTextInput" rows="5" style="width:100%;resize:vertical;border:1px solid #d0d5dd;border-radius:6px;padding:9px;font:inherit" placeholder="如果浏览器不支持本地截图识别，可把 WhatsApp 聊天文字粘贴到这里，然后点“解析文字”。"></textarea><div style="display:flex;justify-content:flex-end;margin-top:8px"><button type="button" class="button secondary small" id="parseAssistTextBtn">解析文字</button></div>';panel.appendChild(box);$('#parseAssistTextBtn').onclick=()=>{const text=$('#assistTextInput').value.trim();if(!text){setStatus('没有可解析的文字','先粘贴客户聊天内容，再点击“解析文字”。',0);return}$('#ocrRawText').value=text;apply(parse(text))}}
  async function nativeRecognize(){if(!currentFile||nativeBusy)return;nativeBusy=true;$('#runOcrBtn').disabled=true;try{if(!('TextDetector'in window)){setStatus('截图已载入','当前浏览器没有可用的本地 OCR。截图可以继续作为建档参考；也可以在下方粘贴聊天文字自动解析。',0);return}setStatus('正在使用浏览器本地识别…','识别过程不上传截图。',20);const bmp=await createImageBitmap(currentFile),detector=new TextDetector(),items=await detector.detect(bmp);bmp.close?.();const text=(items||[]).map(x=>x.rawValue||'').filter(Boolean).join('\n');$('#ocrRawText').value=text;if(!text.trim())throw new Error('没有识别到文字');apply(parse(text))}catch(err){setStatus('本地识别未得到有效结果','截图已经保留，可继续人工录入，或在下方粘贴聊天文字进行自动解析。',0)}finally{nativeBusy=false;$('#runOcrBtn').disabled=false}}
  function loadFile(file){if(!file||!file.type.startsWith('image/'))return;currentFile=file;if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl=URL.createObjectURL(file);$('#screenshotPreview').src=objectUrl;$('#screenshotReview').hidden=false;$('#screenshotDrop').hidden=true;$('#ocrRawText').value='';setStatus('截图已载入','优先尝试浏览器本地识别；如果浏览器不支持，可在下方粘贴聊天文字解析。',0);ensureTextAssist();nativeRecognize()}
  function setMode(mode){$$('.create-tab').forEach(b=>b.classList.toggle('active',b.dataset.createMode===mode));$('#screenshotAssist').hidden=mode!=='screenshot';const source=fields('source');if(source){if(mode==='screenshot')source.value='Facebook Ads → WhatsApp';else if(source.value==='Facebook Ads → WhatsApp')source.value='Manual'}if(mode==='screenshot'){ensureTextAssist();setTimeout(()=>$('#screenshotDrop')?.focus(),30)}}
  $$('.create-tab').forEach(b=>b.addEventListener('click',()=>setMode(b.dataset.createMode)));$('#chooseScreenshotBtn').addEventListener('click',()=>$('#screenshotFile').click());$('#replaceScreenshotBtn').addEventListener('click',()=>$('#screenshotFile').click());$('#runOcrBtn').textContent='尝试本机识别';$('#runOcrBtn').addEventListener('click',nativeRecognize);$('#screenshotFile').addEventListener('change',e=>loadFile(e.target.files?.[0]));
  const drop=$('#screenshotDrop');['dragenter','dragover'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.add('dragover')}));['dragleave','drop'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.remove('dragover')}));drop.addEventListener('drop',e=>loadFile(e.dataTransfer?.files?.[0]));drop.addEventListener('click',e=>{if(!e.target.closest('button'))$('#screenshotFile').click()});document.addEventListener('paste',e=>{if(!modal.classList.contains('open')||$('#screenshotAssist').hidden)return;const file=[...(e.clipboardData?.files||[])].find(f=>f.type.startsWith('image/'))||[...(e.clipboardData?.items||[])].find(i=>i.type.startsWith('image/'))?.getAsFile();if(file){e.preventDefault();loadFile(file)}});
  let pending=null;form.addEventListener('submit',()=>{pending={name:fields('name')?.value||'',source:fields('source')?.value||'Manual',note:fields('note')?.value||'',firstMessage:fields('firstMessage')?.value||''}},true);form.addEventListener('submit',()=>{if(!pending)return;setTimeout(()=>{try{const raw=localStorage.getItem('peptivanta-crm-v2');if(raw){const data=JSON.parse(raw),c=(data.customers||[]).find(x=>x.name===pending.name);if(c){c.source=pending.source||c.source;if(pending.note)c.note=pending.note;if(pending.firstMessage)c.firstMessage=pending.firstMessage;localStorage.setItem('peptivanta-crm-v2',JSON.stringify(data));if(pending.source!=='Manual'||pending.note||pending.firstMessage)setTimeout(()=>location.reload(),120)}}}catch{}pending=null},20)});form.addEventListener('reset',()=>setTimeout(()=>{setMode('manual');currentFile=null;$('#screenshotReview').hidden=true;$('#screenshotDrop').hidden=false;$('#ocrRawText').value='';const input=$('#assistTextInput');if(input)input.value='';setStatus('等待截图','截图只作为建档辅助，保存前请人工确认。',0)},0));
})();