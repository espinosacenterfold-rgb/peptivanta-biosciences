(() => {
  const STORAGE_KEY = "peptivanta-crm-v2";
  const todayISO = () => new Date().toISOString().slice(0,10);
  const offsetDate = d => { const x=new Date(); x.setHours(12,0,0,0); x.setDate(x.getDate()+d); return x.toISOString().slice(0,10); };
  const stageMeta={new:{label:"新询盘",prob:15},contacted:{label:"已首次回复",prob:25},qualifying:{label:"需求确认中",prob:40},catalog:{label:"已发目录/资料",prob:48},quoted:{label:"已报价",prob:62},negotiating:{label:"议价中",prob:72},payment:{label:"待付款",prob:86},fulfillment:{label:"已付款/履约",prob:100},repeat:{label:"待复购/复购",prob:100}};
  const stageOrder=Object.keys(stageMeta), gradeRank={S:5,A:4,B:3,C:2,D:1};
  const seed=()=>({
    teams:[{id:"T-US1",name:"美国一组",manager:"Sophia",level:"一级销售组",status:"正常"},{id:"T-CA1",name:"加拿大组",manager:"Kevin",level:"二级销售组",status:"正常"},{id:"T-MX1",name:"墨西哥组",manager:"Leo",level:"二级销售组",status:"正常"},{id:"T-AU1",name:"澳大利亚组",manager:"Mia",level:"二级销售组",status:"正常"}],
    permissions:[
      {id:"P-SUPER",name:"超级管理员",scope:"全部数据",permissions:["客户查看","客户编辑","负责人转移","跟进管理","销售管道","报价管理","订单管理","成本利润","物流管理","数据分析","数据导出","子账号管理","销售小组管理","权限组管理","系统设置"]},
      {id:"P-L1",name:"一级管理员",scope:"所管理销售组",permissions:["客户查看","客户编辑","负责人转移","跟进管理","销售管道","报价管理","订单管理","物流管理","数据分析","子账号管理","销售小组管理"]},
      {id:"P-L2",name:"二级管理员 / 组长",scope:"本销售组",permissions:["客户查看","客户编辑","跟进管理","销售管道","报价管理","订单查看","物流查看","小组数据分析"]},
      {id:"P-SALES",name:"普通销售",scope:"仅本人客户",permissions:["客户查看","客户编辑","跟进管理","销售管道","报价管理","本人订单查看"]}
    ],
    whatsapp:[{id:"WA-US-01",name:"WA-US-01",number:"+1 *** 1101",owner:"Sophia",status:"正常"},{id:"WA-US-03",name:"WA-US-03",number:"+1 *** 1103",owner:"Kevin",status:"正常"},{id:"WA-MX-01",name:"WA-MX-01",number:"+52 *** 2201",owner:"Leo",status:"正常"},{id:"WA-AU-01",name:"WA-AU-01",number:"+61 *** 3301",owner:"Mia",status:"正常"}],
    accounts:[{id:"U-001",login:"admin",displayName:"Administrator",permissionGroup:"超级管理员",team:"管理层",whatsapp:"—",status:"正常"},{id:"U-002",login:"sophia",displayName:"Sophia",permissionGroup:"一级管理员",team:"美国一组",whatsapp:"WA-US-01",status:"正常"},{id:"U-003",login:"kevin",displayName:"Kevin",permissionGroup:"二级管理员 / 组长",team:"加拿大组",whatsapp:"WA-US-03",status:"正常"},{id:"U-004",login:"leo",displayName:"Leo",permissionGroup:"普通销售",team:"墨西哥组",whatsapp:"WA-MX-01",status:"正常"},{id:"U-005",login:"mia",displayName:"Mia",permissionGroup:"普通销售",team:"澳大利亚组",whatsapp:"WA-AU-01",status:"正常"}],
    customers:[
      {id:"PV-260823-001",name:"Alex Morgan",country:"United States",company:"Independent reseller",contact:"+1 *** 1820",source:"Facebook Ads",product:"Tirzepatide 10mg",stage:"quoted",grade:"A",value:860,owner:"Sophia",team:"美国一组",whatsapp:"WA-US-01",protected:false,risk:"低",traits:["经销商","价格敏感","COA关注"],nextFollowUp:offsetDate(0),lastContact:offsetDate(-1),createdAt:offsetDate(-7),note:"首批测试单，客户正在对比第二家供应商。",timeline:[{date:offsetDate(-1),channel:"WhatsApp",type:"报价",content:"发送 Tirzepatide 10mg 正式报价及 COA，并确认首批数量。",feedback:"价格基本可接受，正在比较另一家供应商。",nextAction:"今天确认对比结果，避免重复推销。",nextDate:offsetDate(0),author:"Sophia"},{date:offsetDate(-4),channel:"WhatsApp",type:"需求确认",content:"确认首批测试规格和目的地。",feedback:"希望小批量开始。",nextAction:"准备报价和质量文件。",nextDate:offsetDate(-1),author:"Sophia"}]},
      {id:"PV-260823-002",name:"Sofia Bennett",country:"Canada",company:"Research supply buyer",contact:"+1 *** 5246",source:"Website",product:"Retatrutide 20mg",stage:"qualifying",grade:"S",value:4200,owner:"Kevin",team:"加拿大组",whatsapp:"WA-US-03",protected:true,risk:"待审核",traits:["机构采购","大额潜客","文件敏感","长期采购"],nextFollowUp:offsetDate(-1),lastContact:offsetDate(-3),createdAt:offsetDate(-11),note:"潜在长期机构客户，先审核资质，不直接承诺样品。",timeline:[{date:offsetDate(-3),channel:"邮件",type:"需求确认",content:"核对公司信息、检测需求和预计月采购范围。",feedback:"要求 COA/HPLC/MS 与样品评估流程。",nextAction:"完成资质复核后给出可提供文件清单。",nextDate:offsetDate(-1),author:"Kevin"}]},
      {id:"PV-260823-003",name:"Daniel Reed",country:"Australia",company:"Private buyer",contact:"+61 *** 903",source:"Facebook Ads",product:"GHK-Cu 50mg",stage:"payment",grade:"A",value:520,owner:"Mia",team:"澳大利亚组",whatsapp:"WA-AU-01",protected:false,risk:"低",traits:["个人客户","付款确认","物流敏感"],nextFollowUp:offsetDate(0),lastContact:offsetDate(0),createdAt:offsetDate(-4),note:"订单已确认，等待付款到账。",timeline:[{date:offsetDate(0),channel:"WhatsApp",type:"付款",content:"发送最终金额、付款信息与订单摘要。",feedback:"客户表示今天安排付款。",nextAction:"晚些时候核对到账。",nextDate:offsetDate(0),author:"Mia"}]},
      {id:"PV-260823-004",name:"Marco Ruiz",country:"Mexico",company:"Wellness retailer",contact:"+52 *** 761",source:"WhatsApp",product:"Tirzepatide 5mg",stage:"new",grade:"B",value:310,owner:"Leo",team:"墨西哥组",whatsapp:"WA-MX-01",protected:false,risk:"低",traits:["零售商","新询盘","MOQ关注"],nextFollowUp:offsetDate(1),lastContact:offsetDate(0),createdAt:offsetDate(0),note:"刚进入的新询盘。",timeline:[{date:offsetDate(0),channel:"WhatsApp",type:"首次咨询",content:"客户询问价格表与 MOQ。",feedback:"尚未说明预计采购数量。",nextAction:"确认用途、规格与数量。",nextDate:offsetDate(1),author:"Leo"}]},
      {id:"PV-260823-005",name:"Emily Carter",country:"United States",company:"Boutique distributor",contact:"+1 *** 4471",source:"Referral",product:"5-Amino-1MQ 50mg",stage:"fulfillment",grade:"A",value:1280,owner:"Sophia",team:"美国一组",whatsapp:"WA-US-01",protected:true,risk:"低",traits:["经销商","已成交","物流跟进","保护客户"],nextFollowUp:offsetDate(4),lastContact:offsetDate(-2),createdAt:offsetDate(-22),note:"运输中，签收后进入复购维护。",timeline:[{date:offsetDate(-2),channel:"WhatsApp",type:"物流",content:"发送最新 Tracking 状态。",feedback:"客户已收到物流信息。",nextAction:"签收后做满意度回访。",nextDate:offsetDate(4),author:"Sophia"}]},
      {id:"PV-260823-006",name:"Noah Williams",country:"United Kingdom",company:"Research customer",contact:"+44 *** 199",source:"Facebook Ads",product:"BPC-157 10mg",stage:"repeat",grade:"B",value:740,owner:"Kevin",team:"加拿大组",whatsapp:"WA-US-03",protected:false,risk:"低",traits:["科研客户","复购客户","现货关注"],nextFollowUp:offsetDate(2),lastContact:offsetDate(-9),createdAt:offsetDate(-52),note:"首单已完成，需要复购唤醒。",timeline:[{date:offsetDate(-9),channel:"WhatsApp",type:"售后",content:"确认包裹签收和订单情况。",feedback:"无物流异常。",nextAction:"本周进行复购回访。",nextDate:offsetDate(2),author:"Kevin"}]},
      {id:"PV-260823-007",name:"Mia Thompson",country:"United States",company:"Individual",contact:"+1 *** 6034",source:"Facebook Ads",product:"Retatrutide 10mg",stage:"quoted",grade:"C",value:260,owner:"Sophia",team:"美国一组",whatsapp:"WA-US-01",protected:false,risk:"低",traits:["个人客户","价格敏感","报价后未回复"],nextFollowUp:offsetDate(-2),lastContact:offsetDate(-6),createdAt:offsetDate(-15),note:"已经轻度回访一次，避免高频追问。",timeline:[{date:offsetDate(-6),channel:"WhatsApp",type:"报价后回访",content:"询问是否需要补充报价或规格信息。",feedback:"未回复。",nextAction:"间隔后再次轻度唤醒。",nextDate:offsetDate(-2),author:"Sophia"}]},
      {id:"PV-260823-008",name:"Unassigned Lead",country:"United States",company:"Website inquiry",contact:"+1 *** 8008",source:"Website",product:"Peptide catalogue",stage:"new",grade:"B",value:0,owner:"未归属",team:"—",whatsapp:"—",protected:false,risk:"待审核",traits:["网站询盘","未归属"],nextFollowUp:offsetDate(1),lastContact:offsetDate(-1),createdAt:offsetDate(-1),note:"来自网站表单，目前没有对应 WhatsApp 负责人。",timeline:[]}
    ],
    orders:[{id:"SO-2608-018",customerId:"PV-260823-005",customer:"Emily Carter",owner:"Sophia",product:"5-Amino-1MQ 50mg",amount:1280,cost:711,payment:"已付款",logistics:"运输中",tracking:"FX-•••-1842",date:offsetDate(-7)},{id:"SO-2608-013",customerId:"PV-260823-006",customer:"Noah Williams",owner:"Kevin",product:"BPC-157 10mg",amount:740,cost:408,payment:"已付款",logistics:"已签收",tracking:"FX-•••-9071",date:offsetDate(-25)},{id:"SO-2608-020",customerId:"PV-260823-003",customer:"Daniel Reed",owner:"Mia",product:"GHK-Cu 50mg",amount:520,cost:286,payment:"待付款",logistics:"待发货",tracking:"—",date:offsetDate(0)}]
  });
  let state=load(),selectedId=null,poolFilter="priority",poolSort="follow",customerFilter="all",settingsTab="accounts",searchTerm="";
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const money=n=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(Number(n||0));
  const fmtDate=iso=>iso?new Intl.DateTimeFormat("zh-CN",{month:"numeric",day:"numeric"}).format(new Date(iso+"T12:00:00")):"未安排";
  const daysBetween=(a,b)=>Math.round((new Date(b+"T12:00:00")-new Date(a+"T12:00:00"))/86400000), daysSince=iso=>iso?Math.max(0,daysBetween(iso,todayISO())):999;
  const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c])), initials=n=>String(n||"?").split(/\s+/).map(x=>x[0]).join("").slice(0,2).toUpperCase();
  function load(){try{const raw=localStorage.getItem(STORAGE_KEY);if(!raw)return seed();const x=JSON.parse(raw);return x.customers&&x.accounts?x:seed()}catch{return seed()}}
  function save(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state))} function toast(t){const el=$("#toast");el.textContent=t;el.classList.add("show");clearTimeout(toast.t);toast.t=setTimeout(()=>el.classList.remove("show"),1800)}
  function stagePill(s){return `<span class="stage-pill ${s}">${esc(stageMeta[s]?.label||s)}</span>`} function tagHtml(t,cls=""){return `<span class="tag ${cls}">${esc(t)}</span>`}
  function autoActivityTag(c){const d=daysSince(c.lastContact);if(d>=14)return{text:"14天沉默",cls:"red"};if(d>=7)return{text:`${d}天未跟进`,cls:"red"};if(d>=3)return{text:`${d}天未跟进`,cls:"orange"};if(c.nextFollowUp===todayISO())return{text:"今日必须跟进",cls:"orange"};if(c.nextFollowUp<todayISO())return{text:`逾期${daysBetween(c.nextFollowUp,todayISO())}天`,cls:"red"};return{text:"跟进正常",cls:"green"}}
  function keyTags(c,limit=5){const a=autoActivityTag(c),tags=[tagHtml(c.grade,`grade-${c.grade}`),tagHtml(stageMeta[c.stage]?.label||c.stage,"blue"),tagHtml(a.text,a.cls)];(c.traits||[]).slice(0,Math.max(0,limit-3)).forEach(t=>tags.push(tagHtml(t,t.includes("保护")?"green":"")));return tags.join("")}
  function dueText(c){if(!c.nextFollowUp)return"未安排";if(c.nextFollowUp<todayISO())return`逾期 ${daysBetween(c.nextFollowUp,todayISO())} 天`;if(c.nextFollowUp===todayISO())return"今天";return fmtDate(c.nextFollowUp)}
  function renderAll(){renderKpis();renderPool();renderCustomers();renderPipeline();renderFollowups();renderOrders();renderReports();renderSettings();renderBadges()}
  function renderKpis(){const t=todayISO(),today=state.customers.filter(c=>c.nextFollowUp===t).length,overdue=state.customers.filter(c=>c.nextFollowUp&&c.nextFollowUp<t).length,stale=state.customers.filter(c=>daysSince(c.lastContact)>=3).length,unowned=state.customers.filter(c=>c.owner==="未归属").length;const data=[{l:"今日必须跟进",v:today,f:"按优先级连续处理",c:"warning"},{l:"逾期跟进",v:overdue,f:"建议优先清零",c:"danger"},{l:"3天+未跟进",v:stale,f:"自动生成沉默标签",c:"warning"},{l:"未归属客户",v:unowned,f:"仅管理员处理",c:""}];$("#kpiGrid").innerHTML=data.map(x=>`<div class="kpi-card ${x.c}"><div class="kpi-label">${x.l}</div><div class="kpi-value">${x.v}</div><div class="kpi-foot">${x.f}</div></div>`).join("")}
  function filteredPool(){let arr=[...state.customers];if(poolFilter==="mine")arr=arr.filter(c=>c.owner==="Sophia");if(poolFilter==="team")arr=arr.filter(c=>c.team==="美国一组");if(poolFilter==="unowned")arr=arr.filter(c=>c.owner==="未归属");if(searchTerm)arr=arr.filter(c=>JSON.stringify(c).toLowerCase().includes(searchTerm));if(poolFilter==="priority")arr.sort((a,b)=>(a.nextFollowUp||"9999").localeCompare(b.nextFollowUp||"9999")||gradeRank[b.grade]-gradeRank[a.grade]||b.value-a.value);else if(poolSort==="value")arr.sort((a,b)=>b.value-a.value);else if(poolSort==="grade")arr.sort((a,b)=>gradeRank[b.grade]-gradeRank[a.grade]);else arr.sort((a,b)=>(a.nextFollowUp||"9999").localeCompare(b.nextFollowUp||"9999"));return arr}
  function renderPool(){const arr=filteredPool();$("#poolCount").textContent=`${arr.length} 位客户`;if(!selectedId&&arr.length)selectedId=arr[0].id;$("#customerPool").innerHTML=arr.map(c=>{const d=daysSince(c.lastContact);return `<div class="pool-card ${selectedId===c.id?"active":""}" data-select-customer="${c.id}"><div class="pool-card-top"><div class="pool-person"><div class="avatar">${initials(c.name)}</div><div><strong>${esc(c.name)}</strong><small>${esc(c.country)} · ${esc(c.owner)} · ${esc(c.team)}</small></div></div><span class="pool-money">${money(c.value)}</span></div><div class="tag-row">${keyTags(c,5)}</div><div class="pool-meta"><span class="${d>=3?"stale":""}">距上次跟进 ${d} 天</span><span class="${c.nextFollowUp<=todayISO()?"due-today":""}">下次：${dueText(c)}</span></div></div>`}).join("")||`<div class="workspace-empty">暂无客户</div>`;renderWorkspace(selectedId)}
  function renderWorkspace(id){const c=state.customers.find(x=>x.id===id),el=$("#customerWorkspace");if(!c){el.innerHTML=`<div class="workspace-empty">从左侧选择一个客户</div>`;return}const d=daysSince(c.lastContact),last=c.timeline?.[0];el.innerHTML=`<div class="customer-hero"><div class="hero-top"><div class="hero-name"><div class="avatar">${initials(c.name)}</div><div><h2>${esc(c.name)}</h2><p>${esc(c.company)} · ${esc(c.country)} · ${esc(c.contact)}</p><div class="tag-row">${keyTags(c,8)}</div></div></div><div class="hero-actions"><button class="button secondary small" data-open-drawer="${c.id}">完整资料</button><button class="button primary small" data-follow-customer="${c.id}">＋ 记录跟进</button></div></div><div class="owner-strip"><div class="owner-stat"><small>负责人</small><strong>${esc(c.owner)}</strong></div><div class="owner-stat"><small>所属小组</small><strong>${esc(c.team)}</strong></div><div class="owner-stat"><small>WhatsApp</small><strong>${esc(c.whatsapp)}</strong></div><div class="owner-stat"><small>距上次跟进</small><strong class="${d>=3?"stale":""}">${d} 天</strong></div><div class="owner-stat"><small>下次跟进</small><strong>${dueText(c)}</strong></div></div></div><div class="customer-section"><div class="section-title-row"><h3>当前销售上下文</h3><span>${c.protected?"保护客户 · 不自动释放":"普通客户"}</span></div><div class="info-grid"><div class="info-item"><small>主要需求</small><b>${esc(c.product)}</b></div><div class="info-item"><small>机会金额</small><b>${money(c.value)}</b></div><div class="info-item"><small>来源</small><b>${esc(c.source)}</b></div><div class="info-item"><small>风险</small><b>${esc(c.risk)}</b></div></div><div class="follow-summary" style="margin-top:9px"><strong>内部备注</strong><p>${esc(c.note)}</p></div></div><div class="customer-section"><div class="section-title-row"><h3>最近一次跟进</h3><span>${last?esc(last.author)+" · "+fmtDate(last.date):"暂无记录"}</span></div>${last?`<div class="timeline-grid"><div class="timeline-box"><small>具体跟进内容</small><p>${esc(last.content)}</p></div><div class="timeline-box"><small>客户反馈</small><p>${esc(last.feedback)}</p></div><div class="timeline-box"><small>下一步动作</small><p>${esc(last.nextAction)}</p></div><div class="timeline-box"><small>方式 / 类型</small><p>${esc(last.channel)} · ${esc(last.type)}</p></div></div>`:`<div class="workspace-empty" style="height:100px">还没有跟进记录</div>`}</div><div class="customer-section"><div class="section-title-row"><h3>跟进历史</h3><span>共 ${c.timeline?.length||0} 条</span></div><div class="timeline">${(c.timeline||[]).map(t=>`<div class="timeline-item"><span class="timeline-dot"></span><div class="timeline-head"><strong>${esc(t.type)} · ${esc(t.channel)}</strong><span>${fmtDate(t.date)} · ${esc(t.author)}</span></div><div class="timeline-grid"><div class="timeline-box"><small>跟了什么</small><p>${esc(t.content)}</p></div><div class="timeline-box"><small>客户反馈</small><p>${esc(t.feedback)}</p></div><div class="timeline-box"><small>下一步</small><p>${esc(t.nextAction)}</p></div><div class="timeline-box"><small>下次跟进</small><p>${fmtDate(t.nextDate)}</p></div></div></div>`).join("")}</div></div>`}
  function filteredCustomers(){let a=[...state.customers];if(customerFilter==="hot")a=a.filter(c=>["S","A"].includes(c.grade));if(customerFilter==="silent")a=a.filter(c=>daysSince(c.lastContact)>=3);if(customerFilter==="quoted")a=a.filter(c=>["quoted","negotiating"].includes(c.stage));if(customerFilter==="unowned")a=a.filter(c=>c.owner==="未归属");if(customerFilter==="protected")a=a.filter(c=>c.protected);if(searchTerm)a=a.filter(c=>JSON.stringify(c).toLowerCase().includes(searchTerm));return a}
  function renderCustomers(){const arr=filteredCustomers();$("#customerResultCount").textContent=`${arr.length} 位客户`;$("#customersBody").innerHTML=arr.map(c=>`<tr><td class="person-cell"><strong>${esc(c.name)}</strong><small>${esc(c.country)} · ${esc(c.contact)}</small></td><td><div class="tag-row" style="margin:0">${keyTags(c,4)}</div></td><td>${esc(c.owner)}</td><td>${esc(c.team)}</td><td>${esc(c.product)}</td><td>${stagePill(c.stage)}</td><td><b class="${daysSince(c.lastContact)>=3?"stale":""}">${daysSince(c.lastContact)}天前</b></td><td>${dueText(c)}</td><td><b>${money(c.value)}</b></td><td><button class="button secondary small" data-open-drawer="${c.id}">详情</button></td></tr>`).join("")}
  function renderPipeline(){const board=$("#kanbanBoard");board.innerHTML=stageOrder.map(s=>{const cs=state.customers.filter(c=>c.stage===s),total=cs.reduce((x,c)=>x+c.value,0);return `<section class="kanban-column" data-stage="${s}"><div class="kanban-head"><div><strong>${stageMeta[s].label}</strong><small>${money(total)}</small></div><span class="kanban-count">${cs.length}</span></div><div class="kanban-cards" data-drop-stage="${s}">${cs.map(c=>`<article class="deal-card" draggable="true" data-drag-id="${c.id}" data-open-drawer="${c.id}"><h3>${esc(c.name)}</h3><p>${esc(c.owner)} · ${esc(c.product)}</p><div class="tag-row">${tagHtml(c.grade,`grade-${c.grade}`)}${tagHtml(autoActivityTag(c).text,autoActivityTag(c).cls)}</div><div class="deal-value">${money(c.value)}</div><div class="deal-foot"><span>${esc(c.team)}</span><span>${dueText(c)}</span></div></article>`).join("")}</div></section>`}).join("");$("#pipelineSummary").textContent=`${state.customers.length} 位客户 · 机会金额 ${money(state.customers.reduce((s,c)=>s+c.value,0))}`;bindDrag()}
  function allFollowups(){return state.customers.flatMap(c=>(c.timeline||[]).map(t=>({...t,customerId:c.id,customer:c.name,owner:c.owner,product:c.product}))).sort((a,b)=>b.date.localeCompare(a.date))}
  function renderFollowups(){const owner=$("#followOwnerFilter")?.value||"all",type=$("#followTypeFilter")?.value||"all",status=$("#followStatusFilter")?.value||"all";let logs=allFollowups();if(owner!=="all")logs=logs.filter(x=>x.owner===owner);if(type!=="all")logs=logs.filter(x=>x.channel===type);if(status!=="all")logs=logs.filter(x=>{const c=state.customers.find(y=>y.id===x.customerId);if(status==="overdue")return c.nextFollowUp<todayISO();if(status==="today")return c.nextFollowUp===todayISO();return c.nextFollowUp>todayISO()});$("#followupLog").innerHTML=logs.map(t=>`<div class="follow-row" data-open-drawer="${t.customerId}"><div class="follow-icon">${t.channel==="WhatsApp"?"WA":t.channel==="电话"?"☎":"✉"}</div><div class="follow-person"><strong>${esc(t.customer)}</strong><small>${esc(t.owner)} · ${fmtDate(t.date)}</small><small>${esc(t.type)}</small></div><div class="follow-body"><strong>具体跟进：${esc(t.content)}</strong><p class="feedback">客户反馈：${esc(t.feedback)}</p><p>下一步：${esc(t.nextAction)}</p></div><div class="follow-next"><b>下次 ${fmtDate(t.nextDate)}</b><span>${esc(t.channel)}</span></div></div>`).join("")||`<div class="workspace-empty" style="height:220px">暂无跟进记录</div>`;const overdue=state.customers.filter(c=>c.nextFollowUp<todayISO()).length,today=state.customers.filter(c=>c.nextFollowUp===todayISO()).length,stale=state.customers.filter(c=>daysSince(c.lastContact)>=3).length;$("#followupFocus").innerHTML=`<h3>跟进重点</h3><div class="focus-metric"><span>逾期客户</span><b>${overdue}</b></div><div class="focus-metric"><span>今日跟进</span><b>${today}</b></div><div class="focus-metric"><span>3天+未联系</span><b>${stale}</b></div><div class="focus-metric"><span>日志总数</span><b>${allFollowups().length}</b></div><p style="font-size:9.5px;color:#667085;line-height:1.6;margin-top:12px">每次跟进必须记录：具体内容、客户反馈、下一步动作和下次日期。</p>`;populateFollowOwnerFilter()}
  function renderOrders(){const paid=state.orders.filter(o=>o.payment==="已付款"),sales=paid.reduce((s,o)=>s+o.amount,0),profit=paid.reduce((s,o)=>s+o.amount-o.cost,0);$("#orderKpis").innerHTML=[["已收订单",money(sales)],["已收订单数",paid.length],["已实现毛利",money(profit)],["运输中",state.orders.filter(o=>o.logistics==="运输中").length]].map(x=>`<div class="mini-kpi"><small>${x[0]}</small><strong>${x[1]}</strong></div>`).join("");$("#ordersBody").innerHTML=state.orders.map(o=>`<tr><td><b>${o.id}</b></td><td>${esc(o.customer)}</td><td>${esc(o.owner)}</td><td>${esc(o.product)}</td><td>${money(o.amount)}</td><td>${money(o.amount-o.cost)}</td><td>${esc(o.payment)}</td><td>${esc(o.logistics)}</td><td>${esc(o.tracking)}</td><td>${fmtDate(o.date)}</td></tr>`).join("")}
  function rankRows(obj){const entries=Object.entries(obj).sort((a,b)=>b[1]-a[1]),max=Math.max(1,...entries.map(x=>x[1]));return entries.map(([k,v])=>`<div class="rank-row"><span>${esc(k)}</span><div class="rank-track"><div class="rank-fill" style="width:${v/max*100}%"></div></div><b>${v}</b></div>`).join("")}
  function renderReports(){const teams={},sources={},owners={},products={};state.customers.forEach(c=>{teams[c.team]=(teams[c.team]||0)+c.value;sources[c.source]=(sources[c.source]||0)+1;owners[c.owner]=(owners[c.owner]||0)+1;products[c.product]=(products[c.product]||0)+1});$("#teamPerformance").innerHTML=rankRows(teams);$("#sourceChart").innerHTML=rankRows(sources);$("#ownerChart").innerHTML=rankRows(owners);$("#productChart").innerHTML=Object.entries(products).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="product-chip"><b>${v}</b> · ${esc(k)}</div>`).join("")}
  function renderSettings(){const el=$("#settingsContent");if(settingsTab==="accounts")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>子账号管理</h2><p>业务备注名会直接显示为客户负责人，不显示“私海”。</p></div><button class="button primary small" id="addAccountBtn">＋ 新增子账号</button></div><div class="table-wrap"><table class="setting-table"><thead><tr><th>登录账号</th><th>业务备注名</th><th>权限组</th><th>销售小组</th><th>WhatsApp</th><th>状态</th></tr></thead><tbody>${state.accounts.map(a=>`<tr><td>${esc(a.login)}</td><td><b>${esc(a.displayName)}</b></td><td>${esc(a.permissionGroup)}</td><td>${esc(a.team)}</td><td>${esc(a.whatsapp)}</td><td><span class="status-pill ${a.status==='正常'?'':'off'}">${esc(a.status)}</span></td></tr>`).join("")}</tbody></table></div></article><aside class="panel"><div class="settings-toolbar"><div><h2>账号逻辑</h2><p>WhatsApp 跟随业务员</p></div></div><div class="system-note">客户创建时自动记录业务员备注名、所属小组和绑定 WhatsApp。业务员不抢未归属客户；未归属客户仅由管理员处理。离职时由管理员执行客户交接。</div></aside></div>`;
    if(settingsTab==="teams")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>销售小组</h2><p>支持一级、二级等组织层级。</p></div><button class="button secondary small" id="addTeamBtn">＋ 新建销售组</button></div><table class="setting-table"><thead><tr><th>销售组</th><th>负责人</th><th>层级</th><th>成员数</th><th>状态</th></tr></thead><tbody>${state.teams.map(t=>`<tr><td><b>${esc(t.name)}</b></td><td>${esc(t.manager)}</td><td>${esc(t.level)}</td><td>${state.accounts.filter(a=>a.team===t.name).length}</td><td><span class="status-pill">${t.status}</span></td></tr>`).join("")}</tbody></table></article><aside class="panel"><div class="settings-toolbar"><div><h2>数据范围</h2></div></div><div class="system-note">超级管理员：全部数据<br>一级管理员：所管理销售组<br>二级管理员 / 组长：本销售组<br>普通销售：仅本人客户</div></aside></div>`;
    if(settingsTab==="permissions")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>权限组配置</h2><p>功能权限与数据范围分开控制。</p></div><button class="button secondary small" id="addPermissionBtn">＋ 新建权限组</button></div><div class="permission-matrix">${state.permissions.map(p=>`<div class="permission-group-card"><div class="permission-group-head"><div><strong>${esc(p.name)}</strong><small>数据范围：${esc(p.scope)}</small></div><button class="button secondary small">编辑</button></div><div class="permission-chips">${p.permissions.map(x=>`<span class="permission-chip on">${esc(x)}</span>`).join("")}</div></div>`).join("")}</div></article><aside class="panel"><div class="settings-toolbar"><div><h2>权限原则</h2></div></div><div class="system-note">正式接入登录后，权限判断放在服务端/D1 API 层，而不是只隐藏前端按钮。当前版本先验证组织和操作模型。</div></aside></div>`;
    if(settingsTab==="whatsapp")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>WhatsApp账号</h2><p>账号跟随业务员，用于客户归属识别。</p></div><button class="button secondary small" id="addWaBtn">＋ 新增 WhatsApp</button></div><table class="setting-table"><thead><tr><th>内部名称</th><th>号码</th><th>绑定业务员</th><th>客户数</th><th>状态</th></tr></thead><tbody>${state.whatsapp.map(w=>`<tr><td><b>${w.name}</b></td><td>${w.number}</td><td>${w.owner}</td><td>${state.customers.filter(c=>c.whatsapp===w.name).length}</td><td><span class="status-pill">${w.status}</span></td></tr>`).join("")}</tbody></table></article><aside class="panel"><div class="settings-toolbar"><div><h2>归属规则</h2></div></div><div class="system-note">客户页面显示“负责人：Kevin”，不显示“归属：私海”。未归属客户显示“负责人：未归属”。保护客户不会因长期未跟进自动释放。</div></aside></div>`;bindSettingsActions()}
  function renderBadges(){$("#customerCountBadge").textContent=state.customers.length;$("#followCountBadge").textContent=state.customers.filter(c=>c.nextFollowUp<=todayISO()).length}
  function renderDrawer(id){const c=state.customers.find(x=>x.id===id);if(!c)return;$("#drawerContent").innerHTML=`<div class="customer-hero"><div class="hero-top"><div class="hero-name"><div class="avatar">${initials(c.name)}</div><div><h2>${esc(c.name)}</h2><p>${esc(c.id)} · ${esc(c.contact)}</p></div></div><button class="icon-button" id="drawerClose">×</button></div><div class="tag-row">${keyTags(c,10)}</div></div><div class="customer-section"><div class="owner-strip"><div class="owner-stat"><small>负责人</small><strong>${esc(c.owner)}</strong></div><div class="owner-stat"><small>销售组</small><strong>${esc(c.team)}</strong></div><div class="owner-stat"><small>WhatsApp</small><strong>${esc(c.whatsapp)}</strong></div><div class="owner-stat"><small>最后跟进</small><strong>${daysSince(c.lastContact)}天前</strong></div><div class="owner-stat"><small>下次跟进</small><strong>${dueText(c)}</strong></div></div></div><div class="customer-section"><div class="section-title-row"><h3>历史跟进</h3><span>${c.timeline?.length||0}条</span></div><div class="timeline">${(c.timeline||[]).map(t=>`<div class="timeline-item"><span class="timeline-dot"></span><div class="timeline-head"><strong>${esc(t.type)} · ${esc(t.channel)}</strong><span>${fmtDate(t.date)} · ${esc(t.author)}</span></div><div class="timeline-grid"><div class="timeline-box"><small>跟了什么</small><p>${esc(t.content)}</p></div><div class="timeline-box"><small>客户反馈</small><p>${esc(t.feedback)}</p></div><div class="timeline-box"><small>下一步</small><p>${esc(t.nextAction)}</p></div><div class="timeline-box"><small>下次跟进</small><p>${fmtDate(t.nextDate)}</p></div></div></div>`).join("")}</div></div>`;$("#drawerBackdrop").classList.add("open");$("#customerDrawer").classList.add("open");$("#drawerClose").onclick=closeDrawer}
  function closeDrawer(){$("#drawerBackdrop").classList.remove("open");$("#customerDrawer").classList.remove("open")} function openModal(id){populateSelects();$("#modalBackdrop").classList.add("open");$(id).classList.add("open")} function closeModals(){$("#modalBackdrop").classList.remove("open");$$('.modal').forEach(x=>x.classList.remove("open"))}
  function populateSelects(){const owners=state.accounts.filter(a=>a.status==="正常"&&a.displayName!=="Administrator");$("#customerOwnerSelect").innerHTML=owners.map(a=>`<option>${esc(a.displayName)}</option>`).join("")+`<option>未归属</option>`;$("#customerTeamSelect").innerHTML=state.teams.map(t=>`<option>${esc(t.name)}</option>`).join("")+`<option>—</option>`;$("#followCustomerSelect").innerHTML=state.customers.map(c=>`<option value="${c.id}">${esc(c.name)} · ${esc(c.owner)}</option>`).join("");$("#followAuthorSelect").innerHTML=owners.map(a=>`<option>${esc(a.displayName)}</option>`).join("");$("#accountPermissionSelect").innerHTML=state.permissions.map(p=>`<option>${esc(p.name)}</option>`).join("");$("#accountTeamSelect").innerHTML=state.teams.map(t=>`<option>${esc(t.name)}</option>`).join("");$("#accountWaSelect").innerHTML=`<option>—</option>`+state.whatsapp.map(w=>`<option>${esc(w.name)}</option>`).join("")}
  function populateFollowOwnerFilter(){const el=$("#followOwnerFilter");if(!el||el.dataset.ready)return;el.innerHTML=`<option value="all">全部负责人</option>`+[...new Set(state.customers.map(c=>c.owner))].map(x=>`<option>${esc(x)}</option>`).join("");el.dataset.ready="1"}
  function bindDrag(){$$('[data-drag-id]').forEach(card=>card.addEventListener('dragstart',e=>e.dataTransfer.setData('text/plain',card.dataset.dragId)));$$('[data-drop-stage]').forEach(col=>{col.addEventListener('dragover',e=>e.preventDefault());col.addEventListener('drop',e=>{e.preventDefault();const id=e.dataTransfer.getData('text/plain'),c=state.customers.find(x=>x.id===id);if(c){c.stage=col.dataset.dropStage;save();renderAll();toast(`已将 ${c.name} 移动到 ${stageMeta[c.stage].label}`)}})})}
  function switchView(v){$$('.view').forEach(x=>x.classList.toggle('active',x.id===`view-${v}`));$$('.nav-item[data-view]').forEach(x=>x.classList.toggle('active',x.dataset.view===v));const titles={dashboard:["客户工作台","按最后跟进时间与销售优先级连续处理客户"],customers:["客户 / 线索","查看客户标签、负责人和跟进状态"],pipeline:["销售管道","固定列宽并在管道内部横向滚动"],followups:["跟进日志","完整记录跟了什么、客户反馈和下一步"],orders:["订单","付款、物流、签收与复购"],reports:["数据分析","销售组、负责人和客户来源"],settings:["组织与权限","子账号、销售组、权限组与 WhatsApp 绑定"]};$("#pageTitle").textContent=titles[v][0];$("#pageSubtitle").textContent=titles[v][1]}
  function bindSettingsActions(){const a=$("#addAccountBtn");if(a)a.onclick=()=>openModal('#accountModal');const team=$("#addTeamBtn");if(team)team.onclick=()=>{const name=prompt('销售组名称');if(!name)return;state.teams.push({id:'T-'+Date.now(),name,manager:'待设置',level:'二级销售组',status:'正常'});save();renderAll();toast('已新增销售组')};const wa=$("#addWaBtn");if(wa)wa.onclick=()=>{const name=prompt('WhatsApp 内部名称，例如 WA-US-05');if(!name)return;state.whatsapp.push({id:'W-'+Date.now(),name,number:'待填写',owner:'待绑定',status:'正常'});save();renderAll();toast('已新增 WhatsApp 账号')};const p=$("#addPermissionBtn");if(p)p.onclick=()=>{const name=prompt('权限组名称');if(!name)return;state.permissions.push({id:'P-'+Date.now(),name,scope:'仅本人客户',permissions:['客户查看','跟进管理']});save();renderAll();toast('已新增权限组')}}
  document.addEventListener('click',e=>{const nav=e.target.closest('[data-view]');if(nav)switchView(nav.dataset.view);const pool=e.target.closest('[data-pool-filter]');if(pool){poolFilter=pool.dataset.poolFilter;$$('[data-pool-filter]').forEach(x=>x.classList.toggle('active',x===pool));renderPool()}const cf=e.target.closest('[data-customer-filter]');if(cf){customerFilter=cf.dataset.customerFilter;$$('[data-customer-filter]').forEach(x=>x.classList.toggle('active',x===cf));renderCustomers()}const sc=e.target.closest('[data-select-customer]');if(sc){selectedId=sc.dataset.selectCustomer;renderPool()}const dr=e.target.closest('[data-open-drawer]');if(dr){selectedId=dr.dataset.openDrawer;renderDrawer(selectedId)}const fc=e.target.closest('[data-follow-customer]');if(fc){openModal('#followModal');$("#followCustomerSelect").value=fc.dataset.followCustomer;$("#followForm [name=nextDate]").value=offsetDate(1)}const st=e.target.closest('[data-settings-tab]');if(st){settingsTab=st.dataset.settingsTab;$$('[data-settings-tab]').forEach(x=>x.classList.toggle('active',x===st));renderSettings()}if(e.target.matches('[data-close-modal]'))closeModals()});
  $("#drawerBackdrop").onclick=closeDrawer;$("#modalBackdrop").onclick=closeModals;$("#addCustomerBtn").onclick=()=>openModal('#customerModal');$("#quickFollowBtn").onclick=()=>{openModal('#followModal');$("#followForm [name=nextDate]").value=offsetDate(1)};$("#poolSort").onchange=e=>{poolSort=e.target.value;renderPool()};$("#globalSearch").oninput=e=>{searchTerm=e.target.value.trim().toLowerCase();renderPool();renderCustomers()};$("#exportBtn").onclick=()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='peptivanta-crm-backup.json';a.click();URL.revokeObjectURL(a.href)};
  $("#customerForm").onsubmit=e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget)),account=state.accounts.find(a=>a.displayName===f.owner),id='PV-'+Date.now().toString().slice(-9);state.customers.unshift({id,name:f.name,country:f.country,company:f.company||'未填写',contact:f.contact||'—',source:'Manual',product:f.product,stage:'new',grade:f.grade,value:Number(f.value||0),owner:f.owner,team:f.owner==='未归属'?'—':f.team,whatsapp:account?.whatsapp||'—',protected:false,risk:'待确认',traits:(f.traits||'').split(',').map(x=>x.trim()).filter(Boolean),nextFollowUp:offsetDate(1),lastContact:todayISO(),createdAt:todayISO(),note:'新建客户，待首次完整跟进。',timeline:[]});selectedId=id;save();closeModals();e.currentTarget.reset();renderAll();toast('客户已创建')};
  $("#followForm").onsubmit=e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget)),c=state.customers.find(x=>x.id===f.customerId);if(!c)return;c.timeline.unshift({date:todayISO(),channel:f.channel,type:f.type,content:f.content,feedback:f.feedback,nextAction:f.nextAction,nextDate:f.nextDate,author:f.author,result:f.result});c.lastContact=todayISO();c.nextFollowUp=f.nextDate;if(f.result==='待付款')c.stage='payment';if(f.result==='已成交')c.stage='fulfillment';if(f.result==='无效')c.grade='D';save();closeModals();e.currentTarget.reset();selectedId=c.id;renderAll();toast('跟进记录已保存')};
  $("#accountForm").onsubmit=e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));state.accounts.push({id:'U-'+Date.now(),login:f.login,displayName:f.displayName,permissionGroup:f.permissionGroup,team:f.team,whatsapp:f.whatsapp,status:f.status});const wa=state.whatsapp.find(w=>w.name===f.whatsapp);if(wa)wa.owner=f.displayName;save();closeModals();e.currentTarget.reset();renderAll();toast('子账号已创建')};
  ['followOwnerFilter','followTypeFilter','followStatusFilter'].forEach(id=>$("#"+id).addEventListener('change',renderFollowups));renderAll();
})();