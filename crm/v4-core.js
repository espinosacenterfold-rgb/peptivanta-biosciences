(() => {
  const STORAGE_KEY = "peptivanta-crm-v2";
  const CURRENT_USER = { name: "Administrator", role: "超级管理员", ownerView: "Sophia", teamView: "美国一组" };
  const todayISO = () => new Date().toISOString().slice(0, 10);
  const offsetDate = d => { const x = new Date(); x.setHours(12,0,0,0); x.setDate(x.getDate()+d); return x.toISOString().slice(0,10); };
  const stageMeta = {new:{label:"新询盘",prob:15},contacted:{label:"已首次回复",prob:25},qualifying:{label:"需求确认中",prob:40},catalog:{label:"已发目录/资料",prob:48},quoted:{label:"已报价",prob:62},negotiating:{label:"议价中",prob:72},payment:{label:"待付款",prob:86},fulfillment:{label:"已付款/履约",prob:100},repeat:{label:"待复购/复购",prob:100}};
  const stageOrder = Object.keys(stageMeta), gradeRank = {S:5,A:4,B:3,C:2,D:1};
  const seed = () => ({
    teams:[{id:"T-US1",name:"美国一组",manager:"Sophia",level:"一级销售组",status:"正常"},{id:"T-CA1",name:"加拿大组",manager:"Kevin",level:"二级销售组",status:"正常"},{id:"T-MX1",name:"墨西哥组",manager:"Leo",level:"二级销售组",status:"正常"},{id:"T-AU1",name:"澳大利亚组",manager:"Mia",level:"二级销售组",status:"正常"}],
    permissions:[{id:"P-SUPER",name:"超级管理员",scope:"全部数据",permissions:["客户查看","客户编辑","负责人转移","跟进管理","销售管道","报价管理","订单管理","成本利润","物流管理","数据分析","数据导出","子账号管理","销售小组管理","权限组管理","系统设置"]},{id:"P-L1",name:"一级管理员",scope:"所管理销售组",permissions:["客户查看","客户编辑","负责人转移","跟进管理","销售管道","报价管理","订单管理","物流管理","数据分析","子账号管理","销售小组管理"]},{id:"P-L2",name:"二级管理员 / 组长",scope:"本销售组",permissions:["客户查看","客户编辑","跟进管理","销售管道","报价管理","订单查看","物流查看","小组数据分析"]},{id:"P-SALES",name:"普通销售",scope:"仅本人客户",permissions:["客户查看","客户编辑","跟进管理","销售管道","报价管理","本人订单查看"]}],
    whatsapp:[{id:"WA-US-01",name:"WA-US-01",number:"+1 *** 1101",owner:"Sophia",status:"正常"},{id:"WA-US-03",name:"WA-US-03",number:"+1 *** 1103",owner:"Kevin",status:"正常"},{id:"WA-MX-01",name:"WA-MX-01",number:"+52 *** 2201",owner:"Leo",status:"正常"},{id:"WA-AU-01",name:"WA-AU-01",number:"+61 *** 3301",owner:"Mia",status:"正常"}],
    accounts:[{id:"U-001",login:"admin",displayName:"Administrator",permissionGroup:"超级管理员",team:"管理层",whatsapp:"—",status:"正常"},{id:"U-002",login:"sophia",displayName:"Sophia",permissionGroup:"一级管理员",team:"美国一组",whatsapp:"WA-US-01",status:"正常"},{id:"U-003",login:"kevin",displayName:"Kevin",permissionGroup:"二级管理员 / 组长",team:"加拿大组",whatsapp:"WA-US-03",status:"正常"},{id:"U-004",login:"leo",displayName:"Leo",permissionGroup:"普通销售",team:"墨西哥组",whatsapp:"WA-MX-01",status:"正常"},{id:"U-005",login:"mia",displayName:"Mia",permissionGroup:"普通销售",team:"澳大利亚组",whatsapp:"WA-AU-01",status:"正常"}],
    customers:[
      {id:"PV-260823-001",name:"Alex Morgan",country:"United States",company:"Independent reseller",customerType:"经销商",contact:"+1 *** 1820",source:"Facebook Ads",product:"Tirzepatide 10mg",stage:"quoted",grade:"A",value:860,owner:"Sophia",team:"美国一组",whatsapp:"WA-US-01",protected:false,risk:"低",traits:["价格敏感","COA关注"],nextFollowUp:offsetDate(0),lastContact:offsetDate(-1),createdAt:offsetDate(-7),note:"首批测试单，客户正在对比第二家供应商。",timeline:[{date:offsetDate(-1),channel:"WhatsApp",type:"报价",content:"发送 Tirzepatide 10mg 正式报价及 COA，并确认首批数量。",feedback:"价格基本可接受，正在比较另一家供应商。",nextAction:"今天确认对比结果，避免重复推销。",nextDate:offsetDate(0),author:"Sophia"},{date:offsetDate(-4),channel:"WhatsApp",type:"需求确认",content:"确认首批测试规格和目的地。",feedback:"希望小批量开始。",nextAction:"准备报价和质量文件。",nextDate:offsetDate(-1),author:"Sophia"}]},
      {id:"PV-260823-002",name:"Sofia Bennett",country:"Canada",company:"Research supply buyer",customerType:"机构采购",contact:"+1 *** 5246",source:"Website",product:"Retatrutide 20mg",stage:"qualifying",grade:"S",value:4200,owner:"Kevin",team:"加拿大组",whatsapp:"WA-US-03",protected:true,risk:"待审核",traits:["大额潜客","文件敏感","长期采购"],nextFollowUp:offsetDate(-1),lastContact:offsetDate(-3),createdAt:offsetDate(-11),note:"潜在长期机构客户，先审核资质，不直接承诺样品。",timeline:[{date:offsetDate(-3),channel:"邮件",type:"需求确认",content:"核对公司信息、检测需求和预计月采购范围。",feedback:"要求 COA/HPLC/MS 与样品评估流程。",nextAction:"完成资质复核后给出可提供文件清单。",nextDate:offsetDate(-1),author:"Kevin"}]},
      {id:"PV-260823-003",name:"Daniel Reed",country:"Australia",company:"Private buyer",customerType:"个人客户",contact:"+61 *** 903",source:"Facebook Ads",product:"GHK-Cu 50mg",stage:"payment",grade:"A",value:520,owner:"Mia",team:"澳大利亚组",whatsapp:"WA-AU-01",protected:false,risk:"低",traits:["付款确认","物流敏感"],nextFollowUp:offsetDate(0),lastContact:offsetDate(0),createdAt:offsetDate(-4),note:"订单已确认，等待付款到账。",timeline:[{date:offsetDate(0),channel:"WhatsApp",type:"付款",content:"发送最终金额、付款信息与订单摘要。",feedback:"客户表示今天安排付款。",nextAction:"晚些时候核对到账。",nextDate:offsetDate(0),author:"Mia"}]},
      {id:"PV-260823-004",name:"Marco Ruiz",country:"Mexico",company:"Wellness retailer",customerType:"零售商",contact:"+52 *** 761",source:"WhatsApp",product:"Tirzepatide 5mg",stage:"new",grade:"B",value:310,owner:"Leo",team:"墨西哥组",whatsapp:"WA-MX-01",protected:false,risk:"低",traits:["新询盘","MOQ关注"],nextFollowUp:offsetDate(1),lastContact:offsetDate(0),createdAt:offsetDate(0),note:"刚进入的新询盘。",timeline:[{date:offsetDate(0),channel:"WhatsApp",type:"首次咨询",content:"客户询问价格表与 MOQ。",feedback:"尚未说明预计采购数量。",nextAction:"确认用途、规格与数量。",nextDate:offsetDate(1),author:"Leo"}]},
      {id:"PV-260823-005",name:"Emily Carter",country:"United States",company:"Boutique distributor",customerType:"经销商",contact:"+1 *** 4471",source:"Referral",product:"5-Amino-1MQ 50mg",stage:"fulfillment",grade:"A",value:1280,owner:"Sophia",team:"美国一组",whatsapp:"WA-US-01",protected:true,risk:"低",traits:["已成交","物流跟进","保护客户"],nextFollowUp:offsetDate(4),lastContact:offsetDate(-2),createdAt:offsetDate(-22),note:"运输中，签收后进入复购维护。",timeline:[{date:offsetDate(-2),channel:"WhatsApp",type:"物流",content:"发送最新 Tracking 状态。",feedback:"客户已收到物流信息。",nextAction:"签收后做满意度回访。",nextDate:offsetDate(4),author:"Sophia"}]},
      {id:"PV-260823-006",name:"Noah Williams",country:"United Kingdom",company:"Research customer",customerType:"科研客户",contact:"+44 *** 199",source:"Facebook Ads",product:"BPC-157 10mg",stage:"repeat",grade:"B",value:740,owner:"Kevin",team:"加拿大组",whatsapp:"WA-US-03",protected:false,risk:"低",traits:["复购客户","现货关注"],nextFollowUp:offsetDate(2),lastContact:offsetDate(-9),createdAt:offsetDate(-52),note:"首单已完成，需要复购唤醒。",timeline:[{date:offsetDate(-9),channel:"WhatsApp",type:"售后",content:"确认包裹签收和订单情况。",feedback:"无物流异常。",nextAction:"本周进行复购回访。",nextDate:offsetDate(2),author:"Kevin"}]},
      {id:"PV-260823-007",name:"Mia Thompson",country:"United States",company:"Individual",customerType:"个人客户",contact:"+1 *** 6034",source:"Facebook Ads",product:"Retatrutide 10mg",stage:"quoted",grade:"C",value:260,owner:"Sophia",team:"美国一组",whatsapp:"WA-US-01",protected:false,risk:"低",traits:["价格敏感","报价后未回复"],nextFollowUp:offsetDate(-2),lastContact:offsetDate(-6),createdAt:offsetDate(-15),note:"已经轻度回访一次，避免高频追问。",timeline:[{date:offsetDate(-6),channel:"WhatsApp",type:"报价后回访",content:"询问是否需要补充报价或规格信息。",feedback:"未回复。",nextAction:"间隔后再次轻度唤醒。",nextDate:offsetDate(-2),author:"Sophia"}]},
      {id:"PV-260823-008",name:"Unassigned Lead",country:"United States",company:"Website inquiry",customerType:"其他",contact:"+1 *** 8008",source:"Website",product:"Peptide catalogue",stage:"new",grade:"B",value:0,owner:"未归属",team:"—",whatsapp:"—",protected:false,risk:"待审核",traits:["网站询盘","未归属"],nextFollowUp:offsetDate(1),lastContact:offsetDate(-1),createdAt:offsetDate(-1),note:"来自网站表单，目前没有对应 WhatsApp 负责人。",timeline:[]}
    ],
    orders:[{id:"SO-2608-018",customerId:"PV-260823-005",customer:"Emily Carter",owner:"Sophia",product:"5-Amino-1MQ 50mg",amount:1280,cost:711,payment:"已付款",logistics:"运输中",tracking:"FX-•••-1842",date:offsetDate(-7)},{id:"SO-2608-013",customerId:"PV-260823-006",customer:"Noah Williams",owner:"Kevin",product:"BPC-157 10mg",amount:740,cost:408,payment:"已付款",logistics:"已签收",tracking:"FX-•••-9071",date:offsetDate(-25)},{id:"SO-2608-020",customerId:"PV-260823-003",customer:"Daniel Reed",owner:"Mia",product:"GHK-Cu 50mg",amount:520,cost:286,payment:"待付款",logistics:"待发货",tracking:"—",date:offsetDate(0)}]
  });
  let state=load(),selectedId=null,poolFilter="priority",poolSort="follow",customerFilter="all",settingsTab="accounts",searchTerm="",detailOpen=false,detailSequenceIds=[],detailSequenceLabel="全部客户";
  const poolFilters={time:"all",type:"all",grade:"all",stage:"all",owner:"all"};
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const money=n=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(Number(n||0));
  const fmtDate=iso=>iso?new Intl.DateTimeFormat("zh-CN",{month:"numeric",day:"numeric"}).format(new Date(iso+"T12:00:00")):"未安排";
  const daysBetween=(a,b)=>Math.round((new Date(b+"T12:00:00")-new Date(a+"T12:00:00"))/86400000),daysSince=iso=>iso?Math.max(0,daysBetween(iso,todayISO())):999;
  const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;","&gt;":"&gt;","'":"&#39;",'"':"&quot;"}[c]||c)),initials=n=>String(n||"?").split(/\s+/).map(x=>x[0]).join("").slice(0,2).toUpperCase();
  function inferType(c){const text=`${c.customerType||""} ${(c.traits||[]).join(" ")} ${c.company||""}`.toLowerCase();if(text.includes("机构")||text.includes("research supply"))return"机构采购";if(text.includes("科研")||text.includes("research customer"))return"科研客户";if(text.includes("经销")||text.includes("distributor")||text.includes("reseller"))return"经销商";if(text.includes("零售")||text.includes("retailer"))return"零售商";if(text.includes("brand"))return"品牌方";if(text.includes("oem")||text.includes("定制"))return"OEM/定制";if(text.includes("个人")||text.includes("individual")||text.includes("private buyer"))return"个人客户";return c.customerType||"其他"}
  function syncDerived(c){if(c.timeline?.length){c.timeline.sort((a,b)=>String(b.date||"").localeCompare(String(a.date||"")));c.lastContact=c.timeline[0].date||c.lastContact;if(c.timeline[0].nextDate)c.nextFollowUp=c.timeline[0].nextDate}c.lastContact||=c.createdAt||todayISO();c.nextFollowUp||=offsetDate(1)}
  function normalizeState(x){const f=seed();x.teams||=f.teams;x.permissions||=f.permissions;x.whatsapp||=f.whatsapp;x.accounts||=f.accounts;x.orders||=[];x.customers||=[];x.customers.forEach(c=>{c.customerType||=inferType(c);c.traits||=[];c.timeline||=[];c.protected=Boolean(c.protected);c.risk||="待确认";c.owner||="未归属";c.team||="—";c.whatsapp||="—";c.stage||="new";c.grade||="B";c.value=Number(c.value||0);c.createdAt||=todayISO();syncDerived(c)});return x}
  function load(){try{const raw=localStorage.getItem(STORAGE_KEY);return normalizeState(raw?JSON.parse(raw):seed())}catch{return normalizeState(seed())}}
  function save(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}function toast(t){const el=$("#toast");el.textContent=t;el.classList.add("show");clearTimeout(toast.t);toast.t=setTimeout(()=>el.classList.remove("show"),1800)}
  function dueText(c){if(!c.nextFollowUp)return"未安排";if(c.nextFollowUp<todayISO())return`逾期 ${daysBetween(c.nextFollowUp,todayISO())} 天`;if(c.nextFollowUp===todayISO())return"今天";return fmtDate(c.nextFollowUp)}
  function automaticTags(c){const tags=[],d=daysSince(c.lastContact);if(d>=14)tags.push({text:"14天沉默",cls:"red"});else if(d>=7)tags.push({text:`${d}天未跟进`,cls:"red"});else if(d>=3)tags.push({text:`${d}天未跟进`,cls:"orange"});if(c.nextFollowUp<todayISO())tags.push({text:`逾期${daysBetween(c.nextFollowUp,todayISO())}天`,cls:"red"});else if(c.nextFollowUp===todayISO())tags.push({text:"今日必须跟进",cls:"orange"});if(c.owner==="未归属")tags.push({text:"未归属",cls:"purple"});if(c.protected)tags.push({text:"保护客户",cls:"green"});return tags}
  function tagHtml(t,cls=""){return`<span class="tag ${cls}">${esc(t)}</span>`}function stagePill(s){return`<span class="stage-pill ${s}">${esc(stageMeta[s]?.label||s)}</span>`}
  function keyTags(c,limit=7){const tags=[{text:c.grade,cls:`grade-${c.grade}`},{text:stageMeta[c.stage]?.label||c.stage,cls:"blue"},...automaticTags(c),{text:c.customerType||inferType(c),cls:""}];(c.traits||[]).forEach(t=>{if(!tags.some(x=>x.text===t))tags.push({text:t,cls:t.includes("保护")?"green":""})});return tags.slice(0,limit).map(x=>tagHtml(x.text,x.cls)).join("")}
  function followAdvice(c){const d=daysSince(c.lastContact),last=c.timeline?.[0],feedback=String(last?.feedback||"");let main="",tone="",focus="",avoid="";if(c.owner==="未归属"){main="先由管理员确认客户应归属的业务员与 WhatsApp，再进入正式跟进。";tone="内部处理，不直接由其他业务员抢单。";focus="核对来源、地区、联系方式与原始询盘内容。";avoid="避免多人同时联系同一客户。"}else if(["new","contacted"].includes(c.stage)){main="先完成需求资格确认，再发送有针对性的资料。";tone="简洁、自然，先问需求，不要一上来堆产品信息。";focus="产品、规格、数量、目的地、采购用途/身份。";avoid="避免直接发长目录或连续追问。"}else if(["qualifying","catalog"].includes(c.stage)){main=c.customerType==="机构采购"?"优先解决资质、检测文件与采购流程问题，再推进报价。":"把尚未确认的规格、数量和关键顾虑补齐后再推进报价。";tone="专业、信息型，不急于催单。";focus=c.customerType==="机构采购"?"COA/HPLC/MS、批次、样品条件、采购规模。":"MOQ、规格、数量、价格区间与物流。";avoid=c.customerType==="机构采购"?"资质未核验前不要直接承诺免费样品。":"不要重复发送客户已经收到的资料。"}else if(["quoted","negotiating"].includes(c.stage)){if(feedback.includes("未回复")||d>=3){main="做一次轻度唤醒，不重复整份报价；从客户上次关注点切入。";tone="低压力、短消息，给客户容易回复的入口。";focus=(c.traits||[]).includes("价格敏感")?"确认是否需要调整数量/规格以匹配预算。":"询问是否还需要补充文件、规格或物流信息。";avoid="避免连续问‘还需要吗/考虑得怎么样’。"}else{main="围绕报价后的具体阻力推进下一步，优先消除一个关键障碍。";tone="明确但不施压。";focus="价格、MOQ、付款、文件或物流中客户当前最在意的一项。";avoid="不要在客户尚未提出异议时主动连续降价。"}}else if(c.stage==="payment"){main="本次只推进付款确认和到账，不再重新介绍产品。";tone="清楚、事务型。";focus="付款方式、金额、到账状态、付款后发货节点。";avoid="避免在付款节点增加新的复杂选择。"}else if(c.stage==="fulfillment"){main="以物流和签收为主，签收后再转售后/复购维护。";tone="服务型，主动但不过度打扰。";focus="Tracking、清关/派送、签收状态与异常。";avoid="包裹未签收前不要过早做复购推销。"}else if(c.stage==="repeat"){main="从上次订单和补货周期切入复购，不要重新做基础产品教育。";tone="熟客式、简短。";focus="库存、补货时间、上次产品反馈、是否需要同规格或组合采购。";avoid="避免把复购客户当新询盘重新走完整介绍流程。"}if(c.risk==="待审核"||c.risk==="高")focus+=" 同时保留风险审核，不在未确认前扩大承诺。";if(c.nextFollowUp<todayISO())main=`【今日优先】${main}`;return{main,tone,focus,avoid}}
  function adviceHtml(c){const a=followAdvice(c);return`<div class="advice-card"><div class="advice-head"><strong>跟进建议</strong><span>根据阶段、最近反馈、跟进时效和标签生成</span></div><div class="advice-main">${esc(a.main)}</div><div class="advice-grid"><div class="advice-box"><small>建议语气</small><p>${esc(a.tone)}</p></div><div class="advice-box"><small>本次重点</small><p>${esc(a.focus)}</p></div><div class="advice-box"><small>建议避免</small><p>${esc(a.avoid)}</p></div><div class="advice-box"><small>依据</small><p>${esc(stageMeta[c.stage]?.label||c.stage)} · 距上次 ${daysSince(c.lastContact)} 天 · ${esc(c.customerType)}</p></div></div></div>`}
  function customerMatchesSearch(c){if(!searchTerm)return true;return[c.name,c.country,c.company,c.customerType,c.contact,c.source,c.product,c.owner,c.team,c.whatsapp,c.note,c.risk,...(c.traits||[])].join(" ").toLowerCase().includes(searchTerm)}
  function passTimeFilter(c){const d=daysSince(c.lastContact);if(poolFilters.time==="due")return c.nextFollowUp===todayISO();if(poolFilters.time==="overdue")return c.nextFollowUp<todayISO();if(poolFilters.time==="stale3")return d>=3;if(poolFilters.time==="stale7")return d>=7;if(poolFilters.time==="silent14")return d>=14;return true}
  function filteredPool(){let arr=[...state.customers].filter(customerMatchesSearch);if(poolFilter==="mine")arr=arr.filter(c=>c.owner===CURRENT_USER.ownerView);if(poolFilter==="team")arr=arr.filter(c=>c.team===CURRENT_USER.teamView);if(poolFilter==="unowned")arr=arr.filter(c=>c.owner==="未归属");arr=arr.filter(passTimeFilter);if(poolFilters.type!=="all")arr=arr.filter(c=>c.customerType===poolFilters.type);if(poolFilters.grade!=="all")arr=arr.filter(c=>c.grade===poolFilters.grade);if(poolFilters.stage!=="all")arr=arr.filter(c=>c.stage===poolFilters.stage);if(poolFilters.owner!=="all")arr=arr.filter(c=>c.owner===poolFilters.owner);const dueScore=c=>c.nextFollowUp?(c.nextFollowUp<todayISO()?0:c.nextFollowUp===todayISO()?1:2):3;if(poolSort==="value")arr.sort((a,b)=>b.value-a.value);else if(poolSort==="grade")arr.sort((a,b)=>gradeRank[b.grade]-gradeRank[a.grade]||b.value-a.value);else if(poolSort==="last")arr.sort((a,b)=>daysSince(b.lastContact)-daysSince(a.lastContact)||gradeRank[b.grade]-gradeRank[a.grade]);else arr.sort((a,b)=>dueScore(a)-dueScore(b)||(a.nextFollowUp||"9999").localeCompare(b.nextFollowUp||"9999")||gradeRank[b.grade]-gradeRank[a.grade]||b.value-a.value);return arr}
  function activeFilterLabels(){const out=[];if(poolFilter!=="priority")out.push({k:"范围",v:{mine:"我的客户",team:"美国一组",unowned:"未归属"}[poolFilter]||poolFilter});if(poolFilters.time!=="all")out.push({k:"时效",v:$("#poolTimeFilter")?.selectedOptions[0]?.textContent||poolFilters.time});if(poolFilters.type!=="all")out.push({k:"类型",v:poolFilters.type});if(poolFilters.grade!=="all")out.push({k:"等级",v:poolFilters.grade});if(poolFilters.stage!=="all")out.push({k:"阶段",v:stageMeta[poolFilters.stage]?.label||poolFilters.stage});if(poolFilters.owner!=="all")out.push({k:"负责人",v:poolFilters.owner});if(searchTerm)out.push({k:"搜索",v:searchTerm});return out}
  function ensureSelectedInSequence(){const seq=filteredPool();if(!seq.length){selectedId=null;return seq}if(!selectedId||!seq.some(c=>c.id===selectedId))selectedId=seq[0].id;return seq}
  function renderKpis(){const t=todayISO(),today=state.customers.filter(c=>c.nextFollowUp===t).length,overdue=state.customers.filter(c=>c.nextFollowUp&&c.nextFollowUp<t).length,stale=state.customers.filter(c=>daysSince(c.lastContact)>=3).length,unowned=state.customers.filter(c=>c.owner==="未归属").length;const data=[{l:"今日必须跟进",v:today,f:"按优先级连续处理",c:"warning"},{l:"逾期跟进",v:overdue,f:"建议优先清零",c:"danger"},{l:"3天+未跟进",v:stale,f:"自动生成时效标签",c:"warning"},{l:"未归属客户",v:unowned,f:"仅管理员处理",c:""}];$("#kpiGrid").innerHTML=data.map(x=>`<div class="kpi-card ${x.c}"><div class="kpi-label">${x.l}</div><div class="kpi-value">${x.v}</div><div class="kpi-foot">${x.f}</div></div>`).join("")}
  function renderFilterOptions(){const typeEl=$("#poolTypeFilter"),ownerEl=$("#poolOwnerFilter"),stageEl=$("#poolStageFilter");if(typeEl){const types=[...new Set(state.customers.map(c=>c.customerType))].sort();typeEl.innerHTML=`<option value="all">全部类型</option>`+types.map(x=>`<option ${poolFilters.type===x?"selected":""}>${esc(x)}</option>`).join("")}if(ownerEl){const owners=[...new Set(state.customers.map(c=>c.owner))].sort();ownerEl.innerHTML=`<option value="all">全部负责人</option>`+owners.map(x=>`<option ${poolFilters.owner===x?"selected":""}>${esc(x)}</option>`).join("")}if(stageEl)stageEl.innerHTML=`<option value="all">全部阶段</option>`+stageOrder.map(s=>`<option value="${s}" ${poolFilters.stage===s?"selected":""}>${stageMeta[s].label}</option>`).join("");const labels=activeFilterLabels();$("#activeFilterSummary").innerHTML=labels.length?labels.map(x=>`<span class="filter-summary-chip">${esc(x.k)}：${esc(x.v)}</span>`).join(""):`<span class="filter-summary-chip">当前：全部客户</span>`}
  function renderPool(){renderFilterOptions();const arr=ensureSelectedInSequence();$("#poolCount").textContent=`${arr.length} 位客户`;$("#customerPool").innerHTML=arr.length?arr.map(c=>{const d=daysSince(c.lastContact);return`<div class="pool-card ${selectedId===c.id?"active":""}" data-select-customer="${c.id}" data-detail-customer="${c.id}"><div class="pool-card-top"><div class="pool-person"><div class="avatar">${initials(c.name)}</div><div><strong>${esc(c.name)}</strong><small>${esc(c.country)} · ${esc(c.owner)} · ${esc(c.team)}</small></div></div><span class="pool-money">${money(c.value)}</span></div><div class="tag-row">${keyTags(c,6)}</div><div class="pool-meta"><span class="${d>=3?"stale":""}">距上次跟进 ${d} 天</span><span class="pool-meta-actions"><span class="${c.nextFollowUp<=todayISO()?"due-today":""}">下次：${dueText(c)}</span><button type="button" class="text-button pool-open-btn" data-open-detail="${c.id}">打开客户</button></span></div></div>`}).join(""):`<div class="workspace-empty">当前筛选条件下没有客户</div>`;renderWorkspace(selectedId);if(detailOpen)renderDetailModal()}
  function customerHeaderHtml(c){return`<div class="customer-hero"><div class="hero-top"><div class="hero-name"><div class="avatar">${initials(c.name)}</div><div><h2>${esc(c.name)}</h2><p>${esc(c.company)} · ${esc(c.country)} · ${esc(c.contact)}</p><div class="tag-row">${keyTags(c,9)}</div></div></div><div class="hero-actions"><button class="button secondary small" data-open-detail="${c.id}">居中详情</button><button class="button primary small" data-follow-customer="${c.id}">＋ 记录跟进</button></div></div><div class="owner-strip"><div class="owner-stat"><small>负责人</small><strong>${esc(c.owner)}</strong></div><div class="owner-stat"><small>所属小组</small><strong>${esc(c.team)}</strong></div><div class="owner-stat"><small>WhatsApp</small><strong>${esc(c.whatsapp)}</strong></div><div class="owner-stat"><small>距上次跟进</small><strong class="${daysSince(c.lastContact)>=3?"stale":""}">${daysSince(c.lastContact)} 天</strong></div><div class="owner-stat"><small>下次跟进</small><strong>${dueText(c)}</strong></div></div></div>`}
  function infoContextHtml(c){return`<div class="customer-section"><div class="section-title-row"><h3>当前销售上下文</h3><span>${c.protected?"保护客户 · 不自动释放":"普通客户"}</span></div><div class="info-grid"><div class="info-item"><small>客户类型</small><b>${esc(c.customerType)}</b></div><div class="info-item"><small>主要需求</small><b>${esc(c.product)}</b></div><div class="info-item"><small>机会金额</small><b>${money(c.value)}</b></div><div class="info-item"><small>来源</small><b>${esc(c.source)}</b></div><div class="info-item"><small>风险</small><b>${esc(c.risk)}</b></div><div class="info-item"><small>销售阶段</small><b>${esc(stageMeta[c.stage]?.label||c.stage)}</b></div></div><div class="follow-summary" style="margin-top:9px"><strong>内部备注</strong><p>${esc(c.note||"暂无备注")}</p></div></div>`}
  function lastFollowHtml(c){const last=c.timeline?.[0];return`<div class="customer-section"><div class="section-title-row"><h3>最近一次跟进</h3><span>${last?esc(last.author)+" · "+fmtDate(last.date):"暂无记录"}</span></div>${last?`<div class="timeline-grid"><div class="timeline-box"><small>具体跟进内容</small><p>${esc(last.content)}</p></div><div class="timeline-box"><small>客户反馈</small><p>${esc(last.feedback)}</p></div><div class="timeline-box"><small>下一步动作</small><p>${esc(last.nextAction)}</p></div><div class="timeline-box"><small>方式 / 类型</small><p>${esc(last.channel)} · ${esc(last.type)}</p></div></div>`:`<div class="workspace-empty" style="height:90px">还没有跟进记录</div>`}</div>`}
  function historyHtml(c,limit=999){return`<div class="customer-section"><div class="section-title-row"><h3>跟进历史</h3><span>共 ${c.timeline?.length||0} 条</span></div><div class="timeline">${(c.timeline||[]).slice(0,limit).map(t=>`<div class="timeline-item"><span class="timeline-dot"></span><div class="timeline-head"><strong>${esc(t.type)} · ${esc(t.channel)}</strong><span>${fmtDate(t.date)} · ${esc(t.author)}</span></div><div class="timeline-grid"><div class="timeline-box"><small>跟了什么</small><p>${esc(t.content)}</p></div><div class="timeline-box"><small>客户反馈</small><p>${esc(t.feedback)}</p></div><div class="timeline-box"><small>下一步</small><p>${esc(t.nextAction)}</p></div><div class="timeline-box"><small>下次跟进</small><p>${fmtDate(t.nextDate)}</p></div></div></div>`).join("")}</div></div>`}
  function renderWorkspace(id){const c=state.customers.find(x=>x.id===id),el=$("#customerWorkspace");if(!c){el.innerHTML=`<div class="workspace-empty">请调整筛选或选择一个客户</div>`;return}el.innerHTML=customerHeaderHtml(c)+`<div class="customer-section"><div class="section-title-row"><h3>跟进建议</h3></div>${adviceHtml(c)}</div>`+infoContextHtml(c)+lastFollowHtml(c)+historyHtml(c,4)}
  function openDetail(id,sequence=null,label=null){const seq=sequence||filteredPool();if(!seq.some(c=>c.id===id))return;detailSequenceIds=seq.map(c=>c.id);detailSequenceLabel=label||(activeFilterLabels().map(x=>`${x.k}:${x.v}`).join(" · ")||"全部客户");selectedId=id;detailOpen=true;$("#detailOverlay").classList.add("open");$("#detailOverlay").setAttribute("aria-hidden","false");renderPool();renderDetailModal()}
  function currentDetailSequence(){const ids=detailSequenceIds.length?detailSequenceIds:filteredPool().map(c=>c.id);return ids.map(id=>state.customers.find(c=>c.id===id)).filter(Boolean)}function closeDetail(){detailOpen=false;$("#detailOverlay").classList.remove("open");$("#detailOverlay").setAttribute("aria-hidden","true")}
  function renderDetailModal(){if(!detailOpen)return;const seq=currentDetailSequence();if(!seq.length){closeDetail();return}let idx=seq.findIndex(c=>c.id===selectedId);if(idx<0){idx=0;selectedId=seq[0].id}const c=seq[idx],prev=seq[idx-1],next=seq[idx+1],last=c.timeline?.[0];$("#sequencePosition").textContent=`${idx+1} / ${seq.length} · ${detailSequenceLabel}`;$("#sequencePrev").disabled=!prev;$("#sequenceNext").disabled=!next;$("#prevCustomerName").textContent=prev?prev.name:"已到第一位";$("#nextCustomerName").textContent=next?next.name:"已到最后一位";$("#detailFollowBtn").dataset.followCustomer=c.id;const advice=followAdvice(c),stageOptions=stageOrder.map(s=>`<option value="${s}" ${c.stage===s?"selected":""}>${esc(stageMeta[s].label)}</option>`).join(""),gradeOptions=["S","A","B","C","D"].map(g=>`<option ${c.grade===g?"selected":""}>${g}</option>`).join("");$("#detailModalBody").innerHTML=`<div class="detail-command"><div class="detail-identity"><div class="avatar">${initials(c.name)}</div><div><h2>${esc(c.name)}</h2><p>${esc(c.company)} · ${esc(c.country)} · ${esc(c.contact)}</p><div class="tag-row">${keyTags(c,8)}</div></div></div><div class="detail-command-actions"><button class="button secondary small" data-copy-contact="${c.id}">复制联系方式</button><button class="button secondary small" data-copy-summary="${c.id}">复制客户摘要</button><button class="button ${c.protected?"secondary":"primary"} small" data-toggle-protected="${c.id}">${c.protected?"取消保护":"设为保护"}</button></div></div><div class="detail-working-grid"><section class="detail-main-column"><div class="detail-section detail-priority"><div class="section-title-row"><h3>当前要做</h3><span>${dueText(c)}</span></div><div class="priority-action">${esc(advice.main)}</div><div class="priority-meta"><span><b>本次重点</b>${esc(advice.focus)}</span><span><b>建议语气</b>${esc(advice.tone)}</span><span><b>避免</b>${esc(advice.avoid)}</span></div></div>${c.firstMessage?`<div class="detail-section"><div class="section-title-row"><h3>客户原始消息</h3><span>${esc(c.source||"")}</span></div><div class="original-message">${esc(c.firstMessage)}</div></div>`:""}<div class="detail-section"><div class="section-title-row"><h3>最近一次跟进</h3><span>${last?`${fmtDate(last.date)} · ${esc(last.author||"")}`:"暂无记录"}</span></div>${last?`<div class="detail-follow-cards"><div><small>跟进内容</small><p>${esc(last.content||"")}</p></div><div><small>客户反馈</small><p>${esc(last.feedback||"")}</p></div><div><small>下一步</small><p>${esc(last.nextAction||"")}</p></div></div>`:`<div class="empty-detail-note">还没有跟进记录，建议先完成首次沟通。</div>`}</div>${historyHtml(c,8).replace('class="customer-section"','class="detail-section"')}</section><aside class="detail-side-column"><div class="detail-side-card"><h3>客户信息</h3><dl><div><dt>负责人</dt><dd>${esc(c.owner)}</dd></div><div><dt>销售组</dt><dd>${esc(c.team)}</dd></div><div><dt>WhatsApp</dt><dd>${esc(c.whatsapp)}</dd></div><div><dt>客户类型</dt><dd>${esc(c.customerType)}</dd></div><div><dt>主要产品</dt><dd>${esc(c.product)}</dd></div><div><dt>机会金额</dt><dd>${money(c.value)}</dd></div><div><dt>最后跟进</dt><dd>${daysSince(c.lastContact)} 天前</dd></div><div><dt>风险</dt><dd>${esc(c.risk)}</dd></div></dl></div><div class="detail-side-card"><h3>快速更新</h3><label><span>销售阶段</span><select id="detailStageSelect">${stageOptions}</select></label><label><span>客户等级</span><select id="detailGradeSelect">${gradeOptions}</select></label><label><span>下次跟进</span><input id="detailNextDate" type="date" value="${esc(c.nextFollowUp||"")}"></label><button class="button primary" data-save-detail-status="${c.id}">保存状态</button></div><div class="detail-side-card"><h3>内部备注</h3><p>${esc(c.note||"暂无备注")}</p></div></aside></div>`}
  function moveDetail(step){const seq=currentDetailSequence(),idx=seq.findIndex(c=>c.id===selectedId),target=seq[idx+step];if(!target)return;selectedId=target.id;renderPool();renderDetailModal();$("#detailModalBody").scrollTop=0}
  function filteredCustomers(){let a=[...state.customers].filter(customerMatchesSearch);if(customerFilter==="hot")a=a.filter(c=>["S","A"].includes(c.grade));if(customerFilter==="silent")a=a.filter(c=>daysSince(c.lastContact)>=3);if(customerFilter==="quoted")a=a.filter(c=>["quoted","negotiating"].includes(c.stage));if(customerFilter==="unowned")a=a.filter(c=>c.owner==="未归属");if(customerFilter==="protected")a=a.filter(c=>c.protected);return a}
  function renderCustomers(){const arr=filteredCustomers();$("#customerResultCount").textContent=`${arr.length} 位客户`;$("#customersBody").innerHTML=arr.map(c=>`<tr data-detail-row="${c.id}"><td class="person-cell"><strong>${esc(c.name)}</strong><small>${esc(c.country)} · ${esc(c.contact)}</small></td><td><div class="tag-row" style="margin:0">${keyTags(c,5)}</div></td><td>${esc(c.owner)}</td><td>${esc(c.team)}</td><td>${esc(c.product)}</td><td>${stagePill(c.stage)}</td><td><b class="${daysSince(c.lastContact)>=3?"stale":""}">${daysSince(c.lastContact)}天前</b></td><td>${dueText(c)}</td><td><b>${money(c.value)}</b></td><td><button class="button secondary small" data-open-list-detail="${c.id}">详情</button></td></tr>`).join("")}
  function renderPipeline(){const board=$("#kanbanBoard");board.innerHTML=stageOrder.map(s=>{const cs=state.customers.filter(c=>c.stage===s),total=cs.reduce((x,c)=>x+c.value,0);return`<section class="kanban-column" data-stage="${s}"><div class="kanban-head"><div><strong>${stageMeta[s].label}</strong><small>${money(total)}</small></div><span class="kanban-count">${cs.length}</span></div><div class="kanban-cards" data-drop-stage="${s}">${cs.map(c=>`<article class="deal-card" draggable="true" data-drag-id="${c.id}" data-pipeline-detail="${c.id}"><h3>${esc(c.name)}</h3><p>${esc(c.owner)} · ${esc(c.product)}</p><div class="tag-row">${tagHtml(c.grade,`grade-${c.grade}`)}${automaticTags(c).slice(0,2).map(x=>tagHtml(x.text,x.cls)).join("")}</div><div class="deal-value">${money(c.value)}</div><div class="deal-foot"><span>${esc(c.team)}</span><span>${dueText(c)}</span></div></article>`).join("")}</div></section>`}).join("");$("#pipelineSummary").textContent=`${state.customers.length} 位客户 · 机会金额 ${money(state.customers.reduce((s,c)=>s+c.value,0))}`;bindDrag()}
  function allFollowups(){return state.customers.flatMap(c=>(c.timeline||[]).map(t=>({...t,customerId:c.id,customer:c.name,owner:c.owner,product:c.product}))).sort((a,b)=>String(b.date).localeCompare(String(a.date)))}
  function renderFollowups(){const owner=$("#followOwnerFilter")?.value||"all",type=$("#followTypeFilter")?.value||"all",status=$("#followStatusFilter")?.value||"all";let logs=allFollowups();if(owner!=="all")logs=logs.filter(x=>x.owner===owner);if(type!=="all")logs=logs.filter(x=>x.channel===type);if(status!=="all")logs=logs.filter(x=>{const c=state.customers.find(y=>y.id===x.customerId);if(status==="overdue")return c.nextFollowUp<todayISO();if(status==="today")return c.nextFollowUp===todayISO();return c.nextFollowUp>todayISO()});$("#followupLog").innerHTML=logs.map(t=>`<div class="follow-row" data-open-list-detail="${t.customerId}"><div class="follow-icon">${t.channel==="WhatsApp"?"WA":t.channel==="电话"?"☎":"✉"}</div><div class="follow-person"><strong>${esc(t.customer)}</strong><small>${esc(t.owner)} · ${fmtDate(t.date)}</small><small>${esc(t.type)}</small></div><div class="follow-body"><strong>具体跟进：${esc(t.content)}</strong><p class="feedback">客户反馈：${esc(t.feedback)}</p><p>下一步：${esc(t.nextAction)}</p></div><div class="follow-next"><b>下次 ${fmtDate(t.nextDate)}</b><span>${esc(t.channel)}</span></div></div>`).join("")||`<div class="workspace-empty" style="height:220px">暂无跟进记录</div>`;const overdue=state.customers.filter(c=>c.nextFollowUp<todayISO()).length,today=state.customers.filter(c=>c.nextFollowUp===todayISO()).length,stale=state.customers.filter(c=>daysSince(c.lastContact)>=3).length;$("#followupFocus").innerHTML=`<h3>跟进重点</h3><div class="focus-metric"><span>逾期客户</span><b>${overdue}</b></div><div class="focus-metric"><span>今日跟进</span><b>${today}</b></div><div class="focus-metric"><span>3天+未联系</span><b>${stale}</b></div><div class="focus-metric"><span>日志总数</span><b>${allFollowups().length}</b></div>`;populateFollowOwnerFilter()}
  function renderOrders(){const paid=state.orders.filter(o=>o.payment==="已付款"),sales=paid.reduce((s,o)=>s+o.amount,0),profit=paid.reduce((s,o)=>s+o.amount-o.cost,0);$("#orderKpis").innerHTML=[["已收订单",money(sales)],["已收订单数",paid.length],["已实现毛利",money(profit)],["运输中",state.orders.filter(o=>o.logistics==="运输中").length]].map(x=>`<div class="mini-kpi"><small>${x[0]}</small><strong>${x[1]}</strong></div>`).join("");$("#ordersBody").innerHTML=state.orders.map(o=>`<tr><td><b>${esc(o.id)}</b></td><td>${esc(o.customer)}</td><td>${esc(o.owner)}</td><td>${esc(o.product)}</td><td>${money(o.amount)}</td><td>${money(o.amount-o.cost)}</td><td>${esc(o.payment)}</td><td>${esc(o.logistics)}</td><td>${esc(o.tracking)}</td><td>${fmtDate(o.date)}</td></tr>`).join("")}
  function rankRows(obj){const entries=Object.entries(obj).sort((a,b)=>b[1]-a[1]),max=Math.max(1,...entries.map(x=>x[1]));return entries.map(([k,v])=>`<div class="rank-row"><span>${esc(k)}</span><div class="rank-track"><div class="rank-fill" style="width:${v/max*100}%"></div></div><b>${typeof v==="number"&&v>999?money(v):v}</b></div>`).join("")}
  function renderReports(){const teams={},sources={},owners={},products={};state.customers.forEach(c=>{teams[c.team]=(teams[c.team]||0)+c.value;sources[c.source]=(sources[c.source]||0)+1;owners[c.owner]=(owners[c.owner]||0)+1;products[c.product]=(products[c.product]||0)+1});$("#teamPerformance").innerHTML=rankRows(teams);$("#sourceChart").innerHTML=rankRows(sources);$("#ownerChart").innerHTML=rankRows(owners);$("#productChart").innerHTML=Object.entries(products).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="product-chip"><b>${v}</b> · ${esc(k)}</div>`).join("")}
  function renderSettings(){const el=$("#settingsContent");if(settingsTab==="accounts")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>子账号管理</h2><p>业务备注名直接作为客户负责人显示。</p></div><button class="button primary small" id="addAccountBtn">＋ 新增子账号</button></div><div class="table-wrap"><table class="setting-table"><thead><tr><th>登录账号</th><th>业务备注名</th><th>权限组</th><th>销售小组</th><th>WhatsApp</th><th>状态</th></tr></thead><tbody>${state.accounts.map(a=>`<tr><td>${esc(a.login)}</td><td><b>${esc(a.displayName)}</b></td><td>${esc(a.permissionGroup)}</td><td>${esc(a.team)}</td><td>${esc(a.whatsapp)}</td><td><span class="status-pill ${a.status==="正常"?"":"off"}">${esc(a.status)}</span></td></tr>`).join("")}</tbody></table></div></article><aside class="panel"><div class="settings-toolbar"><div><h2>账号逻辑</h2><p>WhatsApp 跟随业务员</p></div></div><div class="system-note">客户归属显示业务员备注名，不显示“私海”。未归属客户由管理员处理。</div></aside></div>`;if(settingsTab==="teams")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>销售小组</h2></div></div><table class="setting-table"><thead><tr><th>销售组</th><th>负责人</th><th>层级</th><th>成员数</th><th>状态</th></tr></thead><tbody>${state.teams.map(t=>`<tr><td><b>${esc(t.name)}</b></td><td>${esc(t.manager)}</td><td>${esc(t.level)}</td><td>${state.accounts.filter(a=>a.team===t.name).length}</td><td>${esc(t.status)}</td></tr>`).join("")}</tbody></table></article></div>`;if(settingsTab==="permissions")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>权限组配置</h2></div></div><div class="permission-matrix">${state.permissions.map(p=>`<div class="permission-group-card"><div class="permission-group-head"><div><strong>${esc(p.name)}</strong><small>数据范围：${esc(p.scope)}</small></div></div><div class="permission-chips">${p.permissions.map(x=>`<span class="permission-chip">${esc(x)}</span>`).join("")}</div></div>`).join("")}</div></article></div>`;if(settingsTab==="whatsapp")el.innerHTML=`<div class="settings-grid"><article class="panel settings-card"><div class="settings-toolbar"><div><h2>WhatsApp账号</h2></div></div><table class="setting-table"><thead><tr><th>内部名称</th><th>号码</th><th>绑定业务员</th><th>客户数</th><th>状态</th></tr></thead><tbody>${state.whatsapp.map(w=>`<tr><td><b>${esc(w.name)}</b></td><td>${esc(w.number)}</td><td>${esc(w.owner)}</td><td>${state.customers.filter(c=>c.whatsapp===w.name).length}</td><td>${esc(w.status)}</td></tr>`).join("")}</tbody></table></article></div>`;bindSettingsActions()}
  function renderBadges(){$("#customerCountBadge").textContent=state.customers.length;$("#followCountBadge").textContent=state.customers.filter(c=>c.nextFollowUp<=todayISO()).length}
  function populateSelects(){const owners=state.accounts.filter(a=>a.status==="正常"&&a.displayName!=="Administrator");$("#customerOwnerSelect").innerHTML=owners.map(a=>`<option>${esc(a.displayName)}</option>`).join("")+`<option>未归属</option>`;$("#customerTeamSelect").innerHTML=state.teams.map(t=>`<option>${esc(t.name)}</option>`).join("")+`<option>—</option>`;$("#followCustomerSelect").innerHTML=state.customers.map(c=>`<option value="${c.id}">${esc(c.name)} · ${esc(c.owner)}</option>`).join("");$("#followAuthorSelect").innerHTML=owners.map(a=>`<option>${esc(a.displayName)}</option>`).join("");$("#accountPermissionSelect").innerHTML=state.permissions.map(p=>`<option>${esc(p.name)}</option>`).join("");$("#accountTeamSelect").innerHTML=state.teams.map(t=>`<option>${esc(t.name)}</option>`).join("");$("#accountWaSelect").innerHTML=`<option>—</option>`+state.whatsapp.map(w=>`<option>${esc(w.name)}</option>`).join("")}
  function populateFollowOwnerFilter(){const el=$("#followOwnerFilter");if(!el)return;const current=el.value||"all";el.innerHTML=`<option value="all">全部负责人</option>`+[...new Set(state.customers.map(c=>c.owner))].map(x=>`<option>${esc(x)}</option>`).join("");if([...el.options].some(o=>o.value===current))el.value=current}
  function openModal(id){populateSelects();$("#modalBackdrop").classList.add("open");$(id).classList.add("open")}function closeModals(){$("#modalBackdrop").classList.remove("open");$$(".modal").forEach(x=>x.classList.remove("open"))}
  function bindDrag(){$$("[data-drag-id]").forEach(card=>card.addEventListener("dragstart",e=>{e.dataTransfer.setData("text/plain",card.dataset.dragId);e.stopPropagation()}));$$("[data-drop-stage]").forEach(col=>{col.addEventListener("dragover",e=>e.preventDefault());col.addEventListener("drop",e=>{e.preventDefault();const id=e.dataTransfer.getData("text/plain"),c=state.customers.find(x=>x.id===id);if(c&&c.stage!==col.dataset.dropStage){c.stage=col.dataset.dropStage;save();renderAll();toast(`已将 ${c.name} 移动到 ${stageMeta[c.stage].label}；负责人未改变`)}})})}
  function switchView(v){$$(".view").forEach(x=>x.classList.toggle("active",x.id===`view-${v}`));$$(".nav-item[data-view]").forEach(x=>x.classList.toggle("active",x.dataset.view===v));const titles={dashboard:["客户工作台","筛选客户序列后连续处理，双击或点“打开客户”进入处理窗口"],customers:["客户 / 线索","查看客户标签、负责人和跟进状态"],pipeline:["销售管道","阶段推进与客户归属分离"],followups:["跟进日志","完整记录客户反馈和下一步"],orders:["订单","付款、物流、签收与复购"],reports:["数据分析","销售组、负责人和客户来源"],settings:["组织与权限","子账号、销售组、权限组与 WhatsApp 绑定"]};$("#pageTitle").textContent=titles[v][0];$("#pageSubtitle").textContent=titles[v][1]}
  function bindSettingsActions(){const a=$("#addAccountBtn");if(a)a.onclick=()=>openModal("#accountModal")}
  function renderAll(){state.customers.forEach(syncDerived);renderKpis();renderPool();renderCustomers();renderPipeline();renderFollowups();renderOrders();renderReports();renderSettings();renderBadges()}
  document.addEventListener("click",e=>{const nav=e.target.closest("[data-view]");if(nav)switchView(nav.dataset.view);const pf=e.target.closest("[data-pool-filter]");if(pf){poolFilter=pf.dataset.poolFilter;$$("[data-pool-filter]").forEach(x=>x.classList.toggle("active",x===pf));renderPool()}const cf=e.target.closest("[data-customer-filter]");if(cf){customerFilter=cf.dataset.customerFilter;$$("[data-customer-filter]").forEach(x=>x.classList.toggle("active",x===cf));renderCustomers()}const sc=e.target.closest("[data-select-customer]");if(sc){selectedId=sc.dataset.selectCustomer;renderPool()}const od=e.target.closest("[data-open-detail]");if(od)openDetail(od.dataset.openDetail);const listDetail=e.target.closest("[data-open-list-detail]");if(listDetail){const id=listDetail.dataset.openListDetail,seq=filteredCustomers();openDetail(id,seq,`客户列表：${document.querySelector("[data-customer-filter].active")?.textContent?.trim()||"全部"}`)}const fc=e.target.closest("[data-follow-customer]");if(fc){openModal("#followModal");$("#followCustomerSelect").value=fc.dataset.followCustomer;$("#followForm [name=nextDate]").value=offsetDate(1)}const saveDetail=e.target.closest("[data-save-detail-status]");if(saveDetail){const c=state.customers.find(x=>x.id===saveDetail.dataset.saveDetailStatus);if(c){c.stage=$("#detailStageSelect").value;c.grade=$("#detailGradeSelect").value;c.nextFollowUp=$("#detailNextDate").value||c.nextFollowUp;save();renderAll();renderDetailModal();toast("客户状态已更新")}}const toggleProtected=e.target.closest("[data-toggle-protected]");if(toggleProtected){const c=state.customers.find(x=>x.id===toggleProtected.dataset.toggleProtected);if(c){c.protected=!c.protected;save();renderAll();renderDetailModal();toast(c.protected?"已设为保护客户":"已取消保护")}}const copyContact=e.target.closest("[data-copy-contact]");if(copyContact){const c=state.customers.find(x=>x.id===copyContact.dataset.copyContact);if(c){navigator.clipboard?.writeText(c.contact||"");toast("联系方式已复制")}}const copySummary=e.target.closest("[data-copy-summary]");if(copySummary){const c=state.customers.find(x=>x.id===copySummary.dataset.copySummary);if(c){const a=followAdvice(c),text=`${c.name} | ${c.country} | ${c.customerType} | ${c.product} | ${stageMeta[c.stage]?.label||c.stage} | 负责人:${c.owner} | 下次:${c.nextFollowUp||"未安排"}\n跟进建议:${a.main}`;navigator.clipboard?.writeText(text);toast("客户摘要已复制")}}const st=e.target.closest("[data-settings-tab]");if(st){settingsTab=st.dataset.settingsTab;$$("[data-settings-tab]").forEach(x=>x.classList.toggle("active",x===st));renderSettings()}if(e.target.matches("[data-close-modal]"))closeModals()});
  document.addEventListener("dblclick",e=>{const card=e.target.closest("[data-detail-customer]");if(card){e.preventDefault();openDetail(card.dataset.detailCustomer)}const row=e.target.closest("[data-detail-row]");if(row){const seq=filteredCustomers();openDetail(row.dataset.detailRow,seq,`客户列表：${document.querySelector("[data-customer-filter].active")?.textContent?.trim()||"全部"}`)}const deal=e.target.closest("[data-pipeline-detail]");if(deal){const current=state.customers.find(c=>c.id===deal.dataset.pipelineDetail),seq=current?state.customers.filter(c=>c.stage===current.stage):[];openDetail(deal.dataset.pipelineDetail,seq,current?`销售管道：${stageMeta[current.stage]?.label||current.stage}`:"销售管道")}});
  $("#sequencePrev").onclick=()=>moveDetail(-1);$("#sequenceNext").onclick=()=>moveDetail(1);$("#detailClose").onclick=closeDetail;$("#detailOverlay").addEventListener("click",e=>{if(e.target===$("#detailOverlay"))closeDetail()});document.addEventListener("keydown",e=>{if(detailOpen&&e.key==="ArrowLeft")moveDetail(-1);if(detailOpen&&e.key==="ArrowRight")moveDetail(1);if(detailOpen&&e.key==="Escape")closeDetail()});
  $("#modalBackdrop").onclick=closeModals;$("#addCustomerBtn").onclick=()=>openModal("#customerModal");$("#quickFollowBtn").onclick=()=>{openModal("#followModal");$("#followForm [name=nextDate]").value=offsetDate(1)};$("#poolSort").onchange=e=>{poolSort=e.target.value;renderPool()};$("#globalSearch").oninput=e=>{searchTerm=e.target.value.trim().toLowerCase();renderPool();renderCustomers()};$("#exportBtn").onclick=()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="peptivanta-crm-backup.json";a.click();URL.revokeObjectURL(a.href)};
  ["poolTimeFilter","poolTypeFilter","poolGradeFilter","poolStageFilter","poolOwnerFilter"].forEach(id=>{$("#"+id).addEventListener("change",e=>{const map={poolTimeFilter:"time",poolTypeFilter:"type",poolGradeFilter:"grade",poolStageFilter:"stage",poolOwnerFilter:"owner"};poolFilters[map[id]]=e.target.value;renderPool()})});$("#resetPoolFilters").onclick=()=>{poolFilter="priority";poolSort="follow";Object.assign(poolFilters,{time:"all",type:"all",grade:"all",stage:"all",owner:"all"});$("#poolSort").value="follow";$$("[data-pool-filter]").forEach(x=>x.classList.toggle("active",x.dataset.poolFilter==="priority"));["poolTimeFilter","poolTypeFilter","poolGradeFilter","poolStageFilter","poolOwnerFilter"].forEach(id=>$("#"+id).value="all");renderPool()};
  $("#customerForm").onsubmit=e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget)),account=state.accounts.find(a=>a.displayName===f.owner),id="PV-"+Date.now().toString().slice(-9),timeline=f.firstMessage?[{date:todayISO(),channel:"WhatsApp",type:"首次咨询",content:`客户主动咨询：${f.firstMessage}`,feedback:"待业务员首次回复",nextAction:"确认客户需求、规格、数量和目的地",nextDate:offsetDate(1),author:f.owner||"未归属",result:"等待客户"}]:[];state.customers.unshift({id,name:f.name,country:f.country,company:f.company||"未填写",customerType:f.customerType||"其他",contact:f.contact||"—",source:f.source||"Manual",product:f.product,stage:"new",grade:f.grade,value:Number(f.value||0),owner:f.owner,team:f.owner==="未归属"?"—":f.team,whatsapp:account?.whatsapp||"—",protected:false,risk:"待确认",traits:(f.traits||"").split(",").map(x=>x.trim()).filter(Boolean),nextFollowUp:offsetDate(1),lastContact:todayISO(),createdAt:todayISO(),note:f.note||"新建客户，待首次完整跟进。",firstMessage:f.firstMessage||"",timeline});selectedId=id;save();closeModals();e.currentTarget.reset();renderAll();toast("客户已创建")};
  $("#followForm").onsubmit=e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget)),c=state.customers.find(x=>x.id===f.customerId);if(!c)return;c.timeline.unshift({date:todayISO(),channel:f.channel,type:f.type,content:f.content,feedback:f.feedback,nextAction:f.nextAction,nextDate:f.nextDate,author:f.author,result:f.result});if(f.result==="待付款")c.stage="payment";if(f.result==="已成交")c.stage="fulfillment";if(f.result==="无效")c.grade="D";syncDerived(c);save();closeModals();e.currentTarget.reset();selectedId=c.id;renderAll();if(detailOpen)renderDetailModal();toast("跟进记录已保存")};
  $("#accountForm").onsubmit=e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));state.accounts.push({id:"U-"+Date.now(),login:f.login,displayName:f.displayName,permissionGroup:f.permissionGroup,team:f.team,whatsapp:f.whatsapp,status:f.status});const wa=state.whatsapp.find(w=>w.name===f.whatsapp);if(wa)wa.owner=f.displayName;save();closeModals();e.currentTarget.reset();renderAll();toast("子账号已创建")};["followOwnerFilter","followTypeFilter","followStatusFilter"].forEach(id=>$("#"+id).addEventListener("change",renderFollowups));renderAll();
})();